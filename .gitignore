# Generated convenience build (run `npm run build` to produce). The hand-authored
# css/ and components/ work with no build, so dist/ is not committed.
dist/

# Docs generator output (scripts/build-docs.mjs). examples/components/ is now
# fully generator-owned (issue #31) — the hand-authored pages it used to share
# the directory with live in examples/css/ instead. The generated home page at
# the repo root is build output too.
examples/components/
/index.html

# Dependencies (the system itself has none)
node_modules/
pnpm-lock.yaml

# OS / editor
.DS_Store
*.log

# Internal build-agent contract (not part of the published system)
AGENT_CONTRACT.md

# Playwright visual regression test output (baselines in tests/__screenshots__/
# ARE committed; these directories are generated artifacts and are not).
test-results/
playwright-report/
/.playwright
Capture d'écran*.png
