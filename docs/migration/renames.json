{
  "provenance": {
    "purpose": "Every consumer-visible rename the v1 absorption makes, recorded as it happens.",
    "created_by": "batch 0.1 — release and verification plumbing (LittleBigCode/design-system#4)",
    "read_by": "scripts/build-migration-guide.mjs",
    "rule": "A batch that renames anything a consumer can import, target or reference owes a row here, in the same PR. The batch plan lists the rows each batch owes."
  },
  "schema": {
    "kind": "component | export | class | token | path — what is being renamed",
    "from": "the 0.x name",
    "to": "the 1.0 name, or null when the thing is removed with no replacement",
    "batch": "the batch number that made the change",
    "note": "optional — why, or what a consumer has to do beyond a find-and-replace",
    "notes": "A change a consumer can see that is not yet a rename — recorded so the row it will owe later is not lost. Not read by the generator."
  },
  "renames": [
    {
      "kind": "token",
      "from": "--ds-vert",
      "to": "--ds-vert",
      "batch": "0.3",
      "note": "Value corrected #89fc79 -> #53ff64. The 0.11 hex was eye-dropped from the charter PNG on the belief that the swatch label was a typo; the label was right. Name unchanged — re-check any hard-coded copy of the old hex."
    },
    {
      "kind": "token",
      "from": "--ds-accent (in charts, sparkline, bar-chart series)",
      "to": "--ds-chart-1",
      "batch": "0.3",
      "note": "Data series no longer read the themeable accent. Visually identical at the default theme (--ds-chart-1 is #ff2a00); a theme that overrode --ds-accent to recolour its charts must now override --ds-chart-*."
    }
  ],
  "notes": [
    {
      "kind": "export",
      "subject": "SpeedDial",
      "batch": "1",
      "note": "The source composes its trigger onto its own eight-tone Button; that component holds until batch 7, so the trigger is this repo's IconButton and `tone` accepts only its two variants (`primary`, `danger`). Batch 7 re-wires it back and owes the row."
    },
    {
      "kind": "export",
      "subject": "AutocompleteInput / AutocompleteClear",
      "batch": "1",
      "note": "The source composes onto its own compound InputGroup (InputGroupInput / InputGroupAddon / InputGroupButton); that component holds until batch 7, so these compose onto this repo's InputGroup (`after` slot) and IconButton. Batch 7 re-wires them back and owes the row."
    },
    {
      "kind": "component",
      "subject": "context-menu",
      "batch": "1",
      "note": "The /docs/context-menu route documented `Dropdown` and the absence of a right-click menu. It now documents the real `ContextMenu`. Nothing a consumer imports changed name — `Dropdown` is unmoved and still documented at /docs/dropdown-menu — so this is not a rename row."
    },
    {
      "kind": "export",
      "subject": "AttachmentAction",
      "batch": "2",
      "note": "The source composes onto its own eight-tone Button at variant=\"ghost\" size=\"icon-xs\"; that component holds until batch 7, so it composes onto this repo's IconButton — which requires `label`, since an icon-only action needs an accessible name the source's own ghost button never carried. Batch 7 re-wires it back and owes the row."
    },
    {
      "kind": "export",
      "subject": "ThemeSwitcher",
      "batch": "2",
      "note": "All three variants are re-compositions. `segmented` was the source's own toggle-group (held) and is now this repo's Segmented: the cells carry a visible word beside the glyph rather than an icon-only aria-label, because Segmented takes a label node, and the source's sliding indicator goes with the toggle cells it was pitched against. `cycle` was the source's Button (batch 7) and is now IconButton. `dropdown` was the source's dropdown-menu (held) and is now Base UI's Menu wearing .ds-menu, the same re-wiring batch 1's menubar made. Batch 7 re-wires `cycle` back and owes the row."
    },
    {
      "kind": "export",
      "subject": "Snippet",
      "batch": "2",
      "note": "The source composes the held code-block's `CodeBlockCopyButton`. This repo's CodeBlock baked the same button inline, so landing Snippet extracted it into a real export rather than duplicating the clipboard fallback. `CodeBlockCopyButton` is therefore new public surface on an incumbent, not a rename — CodeBlock's own props and markup are unchanged."
    },
    {
      "kind": "component",
      "subject": "resizable, message-scroller",
      "batch": "2",
      "note": "Admit-CSS-only: the stylesheets ship, the React bindings do not (react-resizable-panels and @shadcn/react are not acquired, #156). Their registry entries declare `exports: []` and their pages print no import line — the .ds-resizable-* and .ds-message-scroller-* classes are the whole contract. Nothing a consumer of 0.x could import is affected, so no rename row is owed."
    },
    {
      "kind": "class",
      "subject": "batch 2 dedupe exceptions",
      "batch": "2",
      "note": "Eight Tailwind literals kept as dedupe exceptions upstream are resolved into real declarations plus a modifier class for each consumer override: .ds-toc--static, .ds-toc-label--tight, .ds-toc-list--tight, .ds-toc-link--current, .ds-bubble-group--loose, .ds-message-scroller-content--tight, .ds-separator--auto, and resizable's h-full/w-full which become the group's own defaults. All are net-new names in a clean namespace, so no 0.x class is renamed."
    }
  ]
}
