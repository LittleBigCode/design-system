# Corrections found during execution

Not a derived artifact. The four files beside this one are **copies** carrying a
provenance header, and correcting a measurement in a copy diverges it from the
source silently. This file is where a batch records what it found instead: a
source defect it fixed forward, or a measurement in the plan that execution
proved wrong.

The rule it serves is [`batch-plan.md`](./batch-plan.md) §"The source ref":
nothing unfreezes the pin, so a defect is fixed forward in the target, inside
the batch that hits it, and recorded here as one line. The accepted cost is that
source and target diverge with no trace outside this file.

One row per finding. `batch` is the batch that found it, not the one that owns
the surface.

| batch | kind | finding |
| --- | --- | --- |
| 14 | plan gap, fixed forward | The issue's move list for "the rest of `examples/`" named `blocks/`, `foundations/`, `templates/`, `demo/`, `streamlit/`, `shell.css`, `docs.js`, `showcase.js`, `registry.js` — it omitted `examples/css/`, 67 tracked, hand-written HTML & CSS pages (the "legacy" group `scripts/build-docs.mjs` cross-links every React doc page to). Real, committed content with nowhere else to go under decision 6 (root becomes package-only), so it moved too, to `site/legacy/css/`, alongside the named items. |
| 14 | plan gap, fixed forward | The issue's two named registry moves (`examples/registry/demos/` and `examples/registry/playgrounds/` → `site/src/registry/`) left `examples/registry/registry.ts` and `playgrounds.ts` behind — the declaration files those two directories are compiled as siblings of (`tsconfig.docs.json`'s `rootDir`, and `playgrounds/*.tsx`'s own `from "../playgrounds.js"` import require it). Moved them to `site/src/registry/` too, in the same commit, so `examples/registry/` doesn't survive the batch as a two-file orphan and `dist/docs/**` — confirmed published via `npm pack --dry-run` — keeps compiling from one intact source tree. `tsconfig.docs.json`'s `rootDir`/`include`, `site/vite.config.ts`'s `@registry` alias, `site/tsconfig.app.json`'s mirror path, `site/plugins/demo-source.ts`'s `REGISTRY` const, `site/tests/harness.ts`'s import and `scripts/build-components-md.mjs`'s `REGISTRY` const all repointed accordingly. Verified: `dist/docs/**` diffed byte-for-byte identical against the pre-move build. |
| 14 | plan gap, fixed forward | `scripts/build-docs.mjs` (invoked at the end of `npm run build`, not named in the issue) writes `examples/components/*.html` — the exact 118 files the issue says to delete — plus root `index.html` and `examples/registry.js`. None of its output is published (checked against `package.json` `files` and the pack manifest), but leaving it in the `build` script would recreate a top-level `examples/` directory on every subsequent build, undoing this batch and failing the new `root-is-package-only` check-contracts assertion. Retired its invocation from `package.json`'s `build` script; `build:docs` (the `tsc` step that produces the published `dist/docs/**`) is untouched and still runs. The script file itself is left in place, now orphaned — its remaining internal paths (`examples/registry/playgrounds`, `examples/css`, `examples/components`) are stale, but nothing calls it any more. Batch 16 ("builds the real CSS examples", per the issue's own text) is the likely place to either rewrite or delete it. |
| 14 | plan gap, fixed forward | The issue's own `check-contracts.mjs` scaffolding enumeration (`.github`, `.git`, `node_modules`, `dist`, `site`, `scripts`, `tests`, `deploy`, `starters`, `.claude`, `.ds-sync`, `.feature-dev`, `ds-bundle`, `test-results`) omits `react/` — the TypeScript source `dist/react` (published) compiles from, the React-layer equivalent of `css/`/`components/` shipping raw. Without it the new 5th assertion would fail on every build. Added `react` to the known-scaffolding set. |
| 14 | consequence, not fixed here | Root's entire Playwright surface — `tests/visual.spec.js`, `tests/a11y.spec.js`, and every `tests/*-regress.spec.ts` (all import `tests/harness.ts`, which does `readdirSync(ROOT, "examples/components")` at module scope) — now crashes at collection time (`ENOENT`, confirmed by direct reproduction, not just inferred) because that directory no longer exists at root. `.github/workflows/a11y.yml` and `visual-tests.yml` run exactly these commands (`npx playwright test tests/a11y.spec.js`, `npm run test:visual`, `npm run test:regress`) and will fail on the next push. Root's webServer also can't be exercised in this execution environment regardless (port 8080 is held by an unrelated pre-existing process, OrbStack). Not fixed here: the content these suites exercise (the buildless showcase + legacy CSS pages) is deliberately unwired staging under `site/legacy/` per this batch's own scope — giving it a real, testable route is batch 15's (routing) and batch 16's ("builds the real CSS examples") job, not a route shape batch 14 has any basis to invent. `site/`'s own equivalent suites (`site/tests/*`) are unaffected — verified: `site`'s `typecheck`, `build`, and one live regress spec (`radio-group-regress.spec.ts`) all pass against the new `/design-system/` base and the relocated registry. |
| 14 | orphan left behind, not fixed here | `deploy/pages-redirect.html` was the root `index.html` `pages.yml` copied into `_site` to redirect `/` to the (now-retired) buildless showcase. The rewritten workflow no longer references it. Left in place — `deploy/` is scaffolding this batch doesn't move, and nothing asked for the file's removal — but it has no remaining consumer. |
| 15 | pre-existing defect, unrelated, found and fixed forward (transient, no diff) | `react/index.tsx` had two barrel-export lines reading `./components/Popover.js` and `./components/Combobox.js` (capital first letter) while every other importer of those two files (`date-picker.tsx`, `date-range-picker.tsx`, `time-picker.tsx`, `multi-select.tsx`) correctly used lowercase — a same-file-different-casing specifier pair TS1149/TS1261 rejects outright, so `npm run build:react` failed clean at the start of this batch, before any batch-15 file was touched. Fixed by lowercasing both specifiers to match the one real on-disk file and every other caller. The committed `HEAD` already had the lowercase form — this was a transient dirty-working-tree state, not a persisted defect — so there is nothing to commit for it here; recorded because it blocked verification and needs no further action. |
| 15 | source defect, fixed forward | `docs-search.tsx`'s `CommandDialog` wrapped `CommandInput`/`CommandList`/... directly as children with no `<Command>` root between them. `command.tsx`'s own `CommandDialog` only renders a `Dialog`/`DialogContent` shell around its children — the `cmdk` context `CommandInput`/`CommandList`/`CommandItem` all read from is established by `<Command>` (`CommandPrimitive`) alone, so the palette had no command-loop context to filter or navigate through it. Restored the `<Command>` wrapper from `migration-source-v1`'s `docs-search.tsx`, keeping this repo's already-real grouped-registry `CommandDialog` usage (batch 13's dropped flat `commands` prop) around it. |
| 15 | hand-rolled chrome, restored per the issue's own mandate | `docs-layout.tsx`'s sidebar was a hand-written `<nav>`/`<details>` tree (`Sidebar`/`Collapsible` didn't exist when it was authored); `anatomy.tsx` and `component-page.tsx` imported a hand-rolled local `Toc` from `site/src/chrome/toc.tsx` duplicating the real, barrel-exported `Toc`/`TocItem`/`TocLabel`/`TocLink`/`TocList`; `docs-search.tsx`'s `⌘K` hint was a raw `<span>` duplicating the real `KbdGroup`. All three replaced with the real components from `@diametral/design-system/react` (landed batch 12, #45); `site/src/chrome/toc.tsx` deleted, no remaining importer. `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` in `example.tsx` were using the barrel's *inline* `Tabs` (the genuine incumbent additive with an `items` prop, per `react/index.tsx`'s own header comment) rather than incoming's composition-based `tabs.tsx` — `tabs.tsx` isn't barrel-exported at all (collides with the inline `Tabs` on the export name, same reason `toggle-group` calls out its own subpath in the barrel comment), so the fix imports from the `@diametral/design-system/react/tabs` subpath instead. `Dialog` (the fourth component the issue names) is already dogfooded transitively through `CommandDialog`. |
| 15 | real display/behaviour bugs, fixed forward (not hand-rolled, just incomplete ports) | Three components already used the real barrel components but with a simplified prop shape that doesn't match their actual API: `playground.tsx`'s `PanelSelect` dropped the `items` map `Select` (Base UI's `Select.Root`) resolves a `{ value, label }` option's *trigger* text from — without it, selecting an object-shaped option (real ones exist: `playgrounds.ts`'s icon and chart-colour rows) shows the raw value once picked, though the list showed the label. Three icon-only "copy" buttons (`playground.tsx`'s code-copy, `component-page.tsx`'s import-line copy, `workbench.tsx`'s reset) used `size="sm"` (a labelled-button size) instead of `size="icon-sm" variant="ghost"`, both valid on `Button` (`react/components/button.tsx`). `overview.tsx` and `component-page.tsx` dropped `Badge`'s `variant` prop (`secondary`/`outline`, both real) and `overview.tsx` swapped `CardDescription` (inside `CardHeader`, real) for a `CardFooter` below `CardContent` — no functional bug, but not what's authored, and `CardFooter` was doing a job `CardDescription` already exists for. All restored to `migration-source-v1`'s versions; `overview.tsx`'s own local `--ds-*` token names (already correctly localized, not the source's English placeholders) were left alone. |
| 15 | pre-existing, confirmed unrelated, not fixed here | `site/tests/a11y.spec.ts` on the restored chrome: `label` (both themes) still fails on the `Slider` gap row 107 already accepted (`label-seats`'s `id` lands on Slider's wrapper, not its inner `<input type="range">` — same node class, different auto-generated id, not a new defect). Two new, real findings, neither touching any file this batch changed: `bubble` (dark) — `[data-variant="secondary"]`'s `.ds-bubble-content` fails color-contrast; `attachment` (dark) — `[data-state="error"]`'s `.ds-attachment-description` fails color-contrast. `test:regress`'s 11 failures (`autocomplete`, `phone-input`, `toggle`, `color-picker` ×4, plus one already named in batch 13's own row) are the same pre-existing set batch 13 already recorded, reproduced identically against none of this batch's files. Left for whoever owns `bubble.tsx`/`attachment.tsx`/the four already-named components. |
| 15 | source defect, fixed forward | `templates/dashboard.html`'s KPI row used `.ds-stat`/`.ds-stat__label`/`.ds-stat__value`/`.ds-stat__delta.is-up`/`.is-down` — none of which exist anywhere in `css/` (confirmed by grep: zero matches). The real, current classes are `.ds-stat-card`/`.ds-stat-card-label`/`.ds-stat-card-value`/`.ds-stat-card-delta--up`/`--down` (`css/components/stat-card.css`) — a flat-hyphen rename that happened after this legacy page was authored, caught only because the three KPI cards rendered as unstyled inline text with no border in a browser check rather than from any build-time signal (no test covers these new routes — see the row below). Fixed forward to the real class names. |
| 15 | plan gap, not fixed here | None of the routes landed this pass — `/installation`, `/theming`, `/foundations`, `/foundations/:slug`, `/blocks`, `/blocks/:slug`, `/templates`, `/templates/:slug` (18 pages total) — are covered by `site/tests/a11y.spec.ts` or `visual.spec.ts`: both build their route lists from `COMPONENT_ROUTES` (registry-derived `/docs/:slug` pages) plus a hardcoded `overview`/curated set, with no mechanism for a hand-added top-level route to register itself. Every page was checked by hand in a real browser (rendered correctly, real components, no console errors) but carries none of the gate's automated coverage — the `.ds-stat` finding above is exactly the kind of defect that gap lets through. Extending either suite to pick up hand-added routes is a real gap for whoever owns them next, not something this batch's own pages should hack around unilaterally. |
| 15 | done | The issue's routing task — wiring `installation`/`theming`/`foundations`/`tokens`/`blocks`/`templates` into real routes — is complete. `site/legacy/`'s content (~2,900 lines across 22 files, none in `migration-source-v1`) is ported into 18 real routes on the restored chrome: `/installation`, `/theming` (parts 1-2); `/foundations` + 11 topic pages (part 3); `/blocks` + 4 blocks and `/templates` + 3 templates (this part). "tokens" (named in the issue, no standalone `site/legacy/` file for it) is covered by installation's "Design tokens" specimen row and `foundations/color`'s semantic-tier section — inventing a 19th page with no source content would be guessing, not porting. Sidebar's Guides group now lists all five. |
| 14 | pre-existing, confirmed unrelated, not fixed here | `check-contracts.mjs`'s class-resolution check fails on 20 pre-existing findings — `menubar.tsx`/`.js` (7), `tabs.tsx`/`.js` (2 each), `theme-switcher.tsx`/`.js` (1 each) reference `ds-menu__item`, `ds-menu__item--danger`, `ds-menu__header`, `ds-menu__divider`, `ds-tabs-trigger`, `ds-tabs-content`, none of which resolve to a defined selector. Confirmed identical (same 20, same files, same classes) with this batch's changes stashed out — pre-existing from batch 13's swaps, untouched by any file this batch moved. Left for whoever owns `menubar`/`tabs`/`theme-switcher`. |
| 14 | pre-existing gap, not touched | `docs/adr/0002-repo-root-is-the-web-root.md` now describes a layout this batch reverses (decision 1, already policy-enacted per its own file) — root is no longer the web root, `site/` is. The issue's doc-update list named `CONTRIBUTING.md`, `README.md` and root `CLAUDE.md` only, not ADRs; left for whoever next touches `docs/adr/`. |
| 4 | source defect, fixed forward | `sparkline.tsx` drew a flat series along the floor of its box rather than through the middle: every point equal makes the range zero, and the normalisation divided by it. Fixed in the absorbed component. |
| 4 | source defect, fixed forward | A focus ring that drew nothing — `outline: 2px solid transparent`, Tailwind's `outline-hidden` idiom — was bound to `:focus-visible` with the charte's real 2px outline. |
| 5 | plan measurement wrong | The dedupe-exception table gives batch 5 **5** exceptions across `funnel-chart`, `radar-chart`, `scatter-chart`, `treemap` and `waterfall-chart`. `bullet-chart.css` carries a sixth, in the same form and for the same reason (`[--bullet-label:7rem]` / `[--bullet-value:4rem]` kept literal so `tailwind-merge` could dedupe `in-table.tsx`'s override). The real count is **6**, and the plan's 36-owed total is **37**. All six are resolved in beta.5 — the rule is that an exception is never carried across, and the count is not what decides that. |
| 5 | source comment wrong, no fix needed | `funnel-chart.tsx` carries a comment saying recharts gates a funnel's labels behind `showLabels = !isAnimating`, that the animation therefore restarts forever, and that "with the animation off they render on the first paint" — and then sets no `isAnimationActive` anywhere in the source. Against the recharts this package pins, the labels render with animation on, so the component is absorbed unchanged and `site/tests/chart-marks.spec.ts` asserts the labels are present under animation, which is the one gate that opts back out of reduced motion. |
| 6 | plan question answered | The "one look owed" on `agenda` and `pagination` (batch-plan §4 batch 6, CSS ledger §6.2). **`agenda` is a strict superset** — same `events`/`emptyMessage` props and the same grouping and sorting, plus a `locale` the incumbent could not have (it hardcoded English weekday and month tables), plus `<section>`/`<h3>`/`<time datetime>` where the incumbent emitted a flat run of divs. **`pagination` is not a superset in shape but loses no capability**: the incumbent's `page`/`pageCount`/`onChange` disappear, but its window logic ships intact as the exported pure function `paginationRange()`, and the parts buy a page that can be a real `<a href>` — crawlable, middle-clickable — which the incumbent's buttons could not be. That is composition replacing configuration, which is the trade every applier in this migration makes, not a regression like #164's `time-picker`. Both land; both stylesheets replace. The recipe for rebuilding the controlled pager is in `docs/migration/from-0.11.md`. |
| 6 | plan item resolved early | The `kbd` / `input-group` cascade inversion (batch-plan §4 batch 6, source [#171](https://github.com/diamorval/design-system-diametral/issues/171)) is **fixed now rather than shipped inverted in beta.6 and beta.7**. The source layers `kbd.css` so `InputGroupAddon`'s Tailwind utility keeps outranking `background: var(--input)`; unlayered, the plain declaration would have won instead. Rather than transcribe the losing declaration, `kbd.css` carries what the utility resolved to as a plain rule — `:where([data-slot="input-group"], .ds-input-group) .ds-kbd { background: color-mix(in oklab, var(--ds-ink-faint) 10%, transparent) }` — and drops the source's `var(--input)` line. Batch 7 has nothing to undo. |
| 6 | plan measurement wrong | `toolbar.css` carried `.ds-toolbar .ds-input { min-width: 200px }`, one of the **five** `.ds-input` descendant overrides batch 7 owes a propagation pass (batch-plan §4 batch 7, listing `color-picker.css:54`, `time-picker.css:13`, `toolbar.css:38`, `date-picker.css:16`, `date-range.css:17`). The wholesale replacement of `toolbar.css` deletes it — the replacement is `.ds-toolbar-input`, which sets its own width. **Batch 7 must reach four sites, not five.** |
| 6 | source omission, fixed forward | The source has **no indeterminate rendering for `progress`** — no stripe, no sliding segment, nothing keyed off Base UI's `data-indeterminate`. Absorbing it verbatim would have dropped a state 0.11 could draw and left `value={null}` as a blank track. `progress.css` keeps the animation, keyed off both `[data-indeterminate]` and 0.11's `--indeterminate` modifier, so the React binding and hand-written HTML land on the same rendering. |
| 6 | source defect, fixed forward | `empty.tsx`'s `EmptyDescription` was typed `React.ComponentProps<"p">` and rendered a `<div>`, and `EmptyMedia` rendered `data-slot="empty-icon"` under the name `media`. Both corrected in the absorbed component: a real `<p>`, and `data-slot="empty-media"`. |
| 6 | scope call, recorded | Three 0.11 renderings are **preserved** rather than replaced, each because a wholesale swap would have unstyled a committed fixture with no React component on either side to re-class it. (a) `table.css` keeps the whole element grammar — `.ds-table th/td`, `--hover`, `__num`, `__name`, `__row-action` — beside the source's part grammar, kept apart by `:not(.ds-table-head)` / `:not(.ds-table-cell)` guards and a `.ds-table-container > .ds-table` rule that drops the frame for the composed component; the plan names this one. (b) `spinner.css` keeps the drawn ring under `:not(svg)`, because the React binding spins a Phosphor glyph and hand-written HTML has no glyph to spin — without it every non-React consumer got an invisible rotating box. (c) `.ds-toolbar-spacer` survives the rename, because a hand-written full-width bar still has free space to eat even though the composed `<Toolbar>` is `width: fit-content`. |
| 6 | scope call, recorded | `stepper.tsx` renders **0.11's classes**, not the source's, because `stepper.css` is a cross-boundary pin held by `Wizard` — the plan requires this. Two consequences: there is **no `StepperSeparator`** (the connector is a `::after` on the step, so a separator element would style nothing), and `stepper.css` gains two additive blocks — `.ds-stepper__content { display: contents }` and a `[data-orientation="vertical"]` grid. Neither can reach `Wizard`, which renders no wrapper and no orientation attribute. |
| 6 | scope call, recorded | `examples/demo/` — the signed-in console — keeps the 0.11 prop shapes through a new demo-local `examples/demo/compat.js`, rather than rewriting ~35 hand-written `React.createElement` call sites onto the parts. Nothing in it ships (`examples/` is not in `package.json`'s `files`), and the component pages under `examples/components/` teach the real API. Rewriting the demo onto the parts is a 1.0 cutover item. |
| 6 | pre-existing, not a batch 6 regression | `tests/a11y.spec.js`'s `a11y: demo (signed-in console)` fails on `batch/5-charts-net-new` as well: the demo loads React from esm.sh at runtime and never reaches its sign-in button without network. Verified by stashing the batch and re-running. |
| 6 | incumbent defect, fixed forward | The incumbent `Checkbox` (held until batch 7) destructured a fixed prop list and dropped everything else, so `aria-label` never reached the `<input>`. A checkbox in a selection column has no visible text, so that is its only possible name — `table/selectable` failed axe's `label` rule on it. The applier now spreads its remaining props onto the input. Two lines, in a held component, because the alternative was a demo that fails the gate. |
| 6 | source rule dropped, deliberately | The source's `Stepper` makes the horizontal rail a tab stop, citing axe's `scrollable-region-focusable`: its own CSS lets the row overflow, and a rail with nothing focusable inside is then unreachable by keyboard. The held stylesheet gives every step `flex: 1 1 0; min-width: 0`, so the row shrinks rather than scrolling and the stop would have led nowhere. Dropped, and `site/tests/stepper-regress.spec.ts` asserts its absence alongside the connector. |
| 6 | global defect, fixed forward | `css/themes/dark.css` overrode every `--ds-*-bg` tint but none of the `--ds-*-ink` shades, which are tuned as dark text on the *light* tints. In the dark theme that left dark ink on a dark tint — `.ds-tag--success` and `.ds-banner--info` measured under 2:1. The four ink tokens now have dark-theme mirrors, each clearing 4.5:1 on its own tint, on `--ds-bg` and on `--ds-surface`. Fixed in the token layer rather than in `banner.css`, because `tag.css` and `chip.css` read the same family and `tag.css` is frozen — one four-line override fixes all three without touching a frozen contract. |
| 6 | pre-existing, newly exposed | The dark-theme `.ds-button--primary` contrast defect ([#15](https://github.com/LittleBigCode/design-system/issues/15)) now shows on `stepper` and `pagination` as well, because both batch 6 demos use a primary button. `button.css` is a frozen file batch 7 owns and is untouched here; the same failure already stands on `wizard`, `split-button`, `alert-dialog`, `toggle-group` and `theme-switcher`, none of which this batch touches. |
| 6 | pre-existing, harness | The docs site's own syntax highlighting (`.code-tokens`) fails axe's colour-contrast rule on **every** dark route, batch 1–5 routes included. It is the docs harness's code-sample rendering, not a shipped component, and it is not in #15's list — worth its own row there rather than being absorbed into a batch. |
| 7 | plan premise wrong, no fix needed | **`ds-open` and `ds-close` are not classes.** The plan and the CSS ledger call them "dead classes in a frozen surface" matched by no rule in `css/`, and ask batch 7 to implement or delete them (batch-plan §4 batch 7, §8 item 2; css-ledger §1). They are **CustomEvent names**: `ds-modal.js:67` and `:70` do `dispatchEvent(new CustomEvent("ds-open"))` and never `classList.add`, and both are documented as events in `components/README.md:37` and `docs/components.md:542`. The measurement was a grep artifact — a `ds-[a-z-]+` scan cannot tell a class literal from an event name. **Neither implemented nor deleted**: they are live public API and there is nothing to style. |
| 7 | plan measurement wrong | The `badge.css` **red strip** (batch-plan §1.4) is a no-op in the target, for the reason batch 0.3 recorded for the eleven held files: the only red in the file is `--accent`'s, it already routes through `--ds-accent` / `--ds-accent-ink`, and the modifier is named for it. The source's own `badge.css` brings no red either — its warning colour is `--destructive`. The 20-red-files count was measured on the source. |
| 7 | plan measurement wrong | The plan lists **16 forward cross-batch import re-wirings** owed their second half here (§4). Two of them have no React to re-wire: `message-scroller` (batch 2) and `carousel` (batch 3) are both **admit-CSS-only** — their stylesheets shipped and their bindings did not, because `@shadcn/react` and `embla-carousel-react` were never acquired. The plan's own §4 batch 2 and batch 3 notes say so; the forward-import table was counted from the source's imports, where the binding does exist. **14 re-wirings were owed and 14 are paid.** |
| 7 | react verdict overridden, recorded | **`icon` holds its React binding**, against the ledger's *source wins*. Same test §1.1 applied to `stat-card`: the premise fails. The source's `Icon` is a three-glyph registry (`bell`, `check`, `copy`) over `@phosphor-icons/react` against the incumbent's 34, and — decisively — it cannot serve `<ds-icon>` at all, which is zero-dependency JavaScript with no React to render and reads the same geometry map. Absorbing it would have swapped one shared map for a React-only registry plus a second copy of the geometry for the web component. What **is** absorbed is the substance of the task: the glyph set moves Lucide → Phosphor across the eight files §1.4 names, every icon *name* unchanged. |
| 7 | source rule dropped, deliberately | The source's `.ds-card` is a **flex column**, and absorbing that would have broken a published surface: `docs/streamlit.md` teaches `<div class="ds-card" style="padding:18px 20px">On track <span class="ds-tag ds-tag--success">Run</span></div>` — inline content with no parts, which a flex column stacks. `.ds-card` is frozen for exactly the case where the consumer supplies the children. The parts are block-level and stack unaided; the only thing the flex bought was the footer's push, which `.ds-card__footer` keeps as `margin-block-start: auto`. The same constraint kept `.ds-panel`'s padding on the root rather than moving it to the parts, because `<ds-panel>` also wraps arbitrary light DOM. |
| 7 | scope call, recorded | **Three class names carry two grammars each**, kept apart the way `table.css` kept its two in beta.6 — by what the element contains, not by load order. `.ds-checkbox` is 0.11's label-around-a-hidden-input when it has a `> .ds-checkbox__box` child and the absorbed Base UI box when it does not; `.ds-switch` splits on `> .ds-switch__track`; `.ds-input-group` on `> .ds-input-group__addon`. Each 0.11 form is rendered by committed fixtures under `examples/` that nothing in this migration re-classes, and `<ds-switch>` writes its form itself. `.ds-select` and `.ds-range` needed no guard: the source defines neither name, so the native wrapper and the native range sit beside `.ds-select-*` and `.ds-slider-*` without collision. |
| 7 | export renamed, recorded | **`Status` changes hands and the panel becomes `StatusPanel`.** 0.11's `Status` is a panel — a solid coloured head with a kicker, heading and subtitle — and the source's is an inline dot-and-label indicator. They are different components that happen to share a class stem, and the ledger gives the *name* to the source. In CSS both ship under `.ds-status`, told apart by whether the element has a `__head`. In React one symbol cannot be two things, so the panel takes the name its props interface (`StatusPanelProps`) and its stylesheet (`status-panel.css`) already used. `<ds-status>` is untouched: it is still the panel, and no web-component consumer moves. |
| 7 | incumbent defect, fixed forward | `react/index.tsx` exported **`type Status`**, the six-tone union. It could not be kept as a deprecated alias beside the new `Status` component: a type alias of that name in the barrel shadows the value for every consumer, and the site's own demos failed to compile on it. Renamed to `StatusTone` and recorded in `renames.json` rather than aliased. |
| 7 | plan item resolved elsewhere | The plan gives batch 7 the `.ds-input` **rename propagation** across five files. Two things reduce it to zero edits. `toolbar.css`'s override left with the wholesale replacement in beta.6 (recorded above), so four sites remain — `color-picker.css:54`, `time-picker.css:13`, `date-picker.css:16`, `date-range.css:17`. And the class is **not renamed**: source and target both call it `.ds-input`, so what moved is the file it is defined in, `field.css` → `input.css`. All four overrides win on specificity rather than load order and `input.css` loads ahead of every one of them, so none of the four was touched. `button-group.css:48` is a fifth descendant reference, added by beta.3 after the measurement, and is unaffected for the same reason. |
| 7 | pre-existing, not a batch 7 regression | `docs/for-claude.md` taught a switch as `<span class="ds-switch"><input type="checkbox" checked><span></span></span>` — the track span carrying no `ds-switch__track` class, so it was already unstyled before this batch. Corrected on the way past, because the `:has()` guard that tells the two switch grammars apart would have made a silent no-op into a visible one. |
| 7 | pre-existing, gap not closed here | `stat-card` is **incumbent holds** (ledger §1.1) and so should have been among the 30 held components batch 0.2 seeded into `registry.ts`. It is in `PENDING` instead, where nothing reads it — so the component ships in 1.0.0 with no route and no gate coverage. Left where it is rather than fixed opportunistically: it belongs to nobody's batch, and §5's cutover step 2 (assert the registry length equals 89 + 30) is the check that will catch it. Recorded here so it is not lost. |
| 7 | incumbent defect, fixed forward | **Issue [#15](https://github.com/LittleBigCode/design-system/issues/15)'s dark-theme contrast half is fixed**, on the batch that owns the files. `--ds-on-accent` is pinned to `--ds-blanc` and the dark theme does not flip it, so on dark's near-white `--ds-ink` every ink-filled surface rendered white on white. `bubble.css` recorded the finding in beta.2 and named `.ds-button--primary` as the outstanding case; `--ds-bg` is the token that inverts with the theme, which is what "the colour that contrasts with the ink" actually means. Applied to `.ds-button`'s `--btn-fg`, `.ds-badge--solid`, both of `checkbox.css`'s check marks, and `button-extras.css`'s loading spinner on the primary fill. The eight button *tones* keep `--ds-blanc` / `--ds-noir`: a tone is a brand colour, not a theme one. `split-button [dark]` is green as a side effect. |
| 7 | source defect, fixed forward | `field.css`'s error text used `var(--ds-danger)`, the base hue, which is tuned as a *fill*: as small text on this system's near-black dark background axe measured it under 4.5:1 on `field [dark]`. Moved to `--ds-danger-ink` — the mirror shade batch 6 added to `themes/dark.css` for exactly this — along with `.ds-field__hint--error` and `--success`, which had the same pairing. |
| 7 | pre-existing, still open | The docs harness's syntax highlighting (`.code-tokens`) fails axe's colour-contrast rule on **every** dark route, which now includes five of batch 7's. Batch 6 recorded it and it is unchanged: it is the docs app's own code-sample chrome, not a shipped component, and it does not appear in [#15](https://github.com/LittleBigCode/design-system/issues/15)'s list. It is the only violation left on this batch's 32 route/theme combinations, and §5's cutover step 1 — a full unfiltered gate — cannot pass until it has its own row there. |
| 8 | plan premise wrong, swap still earned | **The tooltip swap does not add `aria-describedby`, and cannot.** The plan states the defect as "`role="tooltip"` on a sibling span with no `id` and no `aria-describedby` on the trigger" and implies the absorbed component wires the description. Measured on the shipped build, Base UI 1.7.0's `Tooltip` emits **no `aria-describedby` and no `role="tooltip"` at all** — deliberately: its documented model is that a tooltip is a *visual-only* supplement and the trigger must carry its own accessible name closely matching the popup's text. The swap is still earned, by a different route: 0.11 announced an association that did not exist, which is worse than announcing none, and had neither Escape dismissal nor a hoverable popup (WCAG 1.4.13, both halves). `site/tests/tooltip-regress.spec.ts` pins what actually ships — every trigger independently named, no dangling `role="tooltip"`, Escape, hoverable — rather than the mechanism the plan guessed at. `tooltip/basic.tsx`'s trigger was relabelled on the way past: it said `aria-label="Delete"` under a tooltip reading "Move to archive", which gives the two audiences two different answers. |
| 8 | scope call, recorded | **The 0.11 drawer and tooltip grammars are preserved beside the absorbed ones**, the third and fourth instance of the call batch 6 recorded for `table.css`, `spinner.css` and `.ds-toolbar-spacer`. The ledger rules both files *wholesale replacement* on the test "not read by an external surface", which is about npm consumers; `examples/components/drawer.html`, `examples/components/tooltip.html`, `examples/kitchen-sink.html` and `examples/demo/views/files.js` write both grammars by hand, and **no React component on either side can re-class them**. `kitchen-sink.html` is in both root gates, so a wholesale swap would have unstyled a fixture the visual suite is about to baseline. Tooltip needed no guard — 0.11 is `.ds-tooltip` / `.ds-tooltip-host`, the source is `.ds-tooltip-content` / `-positioner` / `-arrow`, and the two share no name. Drawer shares exactly one, `.ds-drawer-overlay`, told apart by `:has(> .ds-drawer)` — beta.7's mechanism, on the same reasoning. `kanban` is the opposite case and IS replaced wholesale, because the plan gives this batch its three readers to re-class. |
| 8 | plan measurement incomplete | The plan lists **three** `examples/` files to re-class, all of them kanban's. Six more read a class this batch's ledger rows replace — `examples/components/drawer.html`, `examples/components/tooltip.html`, `examples/components/skeleton.html`, `examples/kitchen-sink.html`, `examples/demo/views/files.js`, plus `docs/components.md`, which is generated. None are re-classed, because the row above preserves their grammars instead; the three kanban files are re-classed exactly as the plan asks. Recorded because the count "three" was measured from the rename list, not from the readers. |
| 8 | source component unusable as absorbed, fixed forward | The source's `Skeleton` is a shadcn component sized entirely by Tailwind utilities (`h-4 w-full`), and **this package ships no utilities** — the same finding `breadcrumb.tsx`, `pagination.tsx` and `carousel.css` recorded for `sr-only`. Absorbed verbatim, `<Skeleton />` with no `className` renders a zero-height box, where 0.11's binding defaulted to `variant="line"`. `skeleton.css` — a HELD file — gains one additive rule giving an unmodified `.ds-skeleton` the line size, scoped `:not(:where([class*="ds-skeleton--"]))`. Both halves of that selector are load-bearing: without the `:not()` a bare rule placed after the modifiers silently outranks `--text`, which is the class `DataGrid` renders and the only reason the file is held; without the `:where()` the selector scores 0,2,0 and outranks any single-class rule a consumer writes to size one. Same shape as beta.6's two additive blocks on held `stepper.css`: it cannot reach the pinning reader, which always writes a modifier alongside. |
| 8 | gate precondition verified | **`DataGrid`'s loading state still renders**, the cross-boundary pin's check. `DataGrid.tsx:321` writes `"ds-skeleton ds-skeleton--text"` as a **string literal** and never composes the `Skeleton` component, so swapping the React binding cannot reach it by any path; `skeleton.css` is held and both classes are untouched. The pin is on the stylesheet alone. |
| 8 | dedupe exception resolved, not carried | `sheet.css`'s one exception was the source keeping `bg-popover`, `text-popover-foreground` and two `data-[side]:w-3/4` literals on `sheet.tsx`, because its `sidebar.tsx` overrode all three on a mobile `SheetContent` and `tailwind-merge` can only dedupe literals it can see. `sidebar` is incumbent-holds — the file never lands, only two cherry-picks onto `AppShell` do — so the override that made the exception necessary does not exist here. All four declarations came home to the stylesheet and `sheet.tsx` carries no Tailwind literals, which also finishes #157's C7: the two-file literal count is now zero. |
| 8 | source pattern adapted, recorded | **`sidebar`'s cookie cherry-pick is write-only in the source.** `sidebar.tsx` sets `sidebar_state` and never reads it: it is a Next.js pattern where a server component reads the cookie and feeds it back as `defaultOpen`. This package is buildless and has no server, so porting the write alone would have persisted nothing. `AppShell` reads it too, in a lazy initialiser, and only to seed the **uncontrolled** default — a caller passing `collapsed` still owns the state outright. The `⌘B` cherry-pick ports as-is, and the toggle gained `aria-keyshortcuts` so the shortcut is discoverable rather than merely present. |
| 8 | docs-harness defect, root-caused and fixed forward | **The `.code-tokens` colour-contrast failure is fixed**, at its cause. Batches 6 and 7 both recorded it as "the docs harness's own syntax highlighting" and deferred it, and beta.7's row named it the one thing blocking §5's cutover step 1. Measured rather than described, the failing spans were `#ff2a00` on **`#676768`** — and `shiki-theme.ts` computes every dark role against "`bg-muted`, i.e. `--ds-bg-alt` (#1b1c1f)". The surface was not the one the palette was tuned for. Cause: `chrome.css` bound `--color-muted: var(--ds-grey-bg)`, and **the dark theme overrides `--ds-bg-alt` but not `--ds-grey-bg`** (#ececec), so every `bg-muted` surface in the docs app was frozen at a near-white grey in dark — precisely what that block's own `@theme inline` note exists to prevent. One binding, `--color-muted: var(--ds-bg-alt)`, fixes every reader at once. The palette needed no edit: its ratios were right about the surface it named. |
| 8 | verified, no regression | The full unfiltered a11y suite (238 tests, every route x both themes) was run on this branch and on a clean worktree at the beta.7 commit, and the failure sets diffed by test name. **beta.7: 151 passed / 77 failed. beta.8: 216 passed / 22 failed. Zero newly-failing tests.** The 55 newly passing are all dark routes freed by the `bg-muted` fix above. The 22 that remain fail identically on both commits and belong to other batches: `aria-allowed-attr` on `time-picker` and `date-time-picker`, `label` on `tags-input` and `direction`, `aria-toggle-field-name` on `collapsible`, `label` and `marker`, and the dark-theme contrast of [#15](https://github.com/LittleBigCode/design-system/issues/15) on `.ds-button--danger`, `.ds-button--link`, `.ds-segmented` and four others. Recorded rather than fixed: none is this batch's surface, and §5's cutover step 1 is the gate that owns them. |
| 8 | pre-existing, not a batch 8 regression | Four assertions fail in `site/tests/batch1-rewiring-regress.spec.ts` (autocomplete's clear button) and `site/tests/batch3-rewiring-regress.spec.ts` (three on phone-input's native select). Verified pre-existing by running both specs in a clean worktree at `batch/7-frozen-contracts` HEAD: **the same four, by name, 47 passed / 4 failed on the baseline.** A worktree rather than a stash round-trip — beta.7 recorded that case-only renames corrupt through a stash on a case-insensitive filesystem, and this batch carries four of them. Not fixed here: they belong to batches 1 and 3, and the cutover's full unfiltered gate is where they are owed. |
| 8 | pre-existing, gap not closed here | **Fifteen batch-6 components have a `PLAYGROUNDS` entry and no playground file**, still sitting in `site/src/registry/_pending/playgrounds/` — `agenda`, `avatar`, `banner`, `breadcrumb`, `description-list`, `empty`, `kbd`, `page-header`, `pagination`, `progress`, `spinner`, `stepper`, `table`, `timeline`, `toolbar`. `playground-registry.ts` skips a slug with no file, so each of those pages renders its examples with no workbench. Batch 6 moved the demos and not the playgrounds; batch 7 did not notice. Left where they are for the same reason beta.7 left `stat-card`: they belong to nobody's batch, and moving them is a one-line `git mv` each that the cutover can do knowingly. This batch moved its own five. |
| 8 | queue spent, two items outstanding | With batch 8 resolved the `ready-for-agent` queue is empty, so the two items recorded as "belongs to nobody's batch" now belong to §5's cutover and to nothing else: **`stat-card`** is incumbent-holds and still sits in `PENDING`, where nothing reads it, so it would ship in 1.0.0 with no route and no gate coverage (beta.7's row); and the **fifteen stranded playgrounds** above. Cutover step 2 — assert `registry.ts` length equals 89 absorbed + 30 held — catches the first by construction. The second has no assertion behind it and is recorded here so it is not lost. |
| 8 | earlier correction wrong, root cause found | **Batch 6's diagnosis of the `a11y: demo (signed-in console)` failure is incorrect.** Its row says the demo "loads React from esm.sh at runtime and never reaches its sign-in button without network". It fails **with** network too, which is why it fails on CI, where the network is fine. The real cause is that `examples/demo.html`'s import map declares `react`, `react-dom`, `react-dom/client` and `recharts` and nothing else, while `dist/react/` is compiled with the automatic JSX runtime: **83 of its modules import `react/jsx-runtime`**, an unmapped bare specifier, so the barrel's module graph fails to resolve and the app never mounts. Behind it sit three more unmapped families — `@base-ui/react/*`, `@phosphor-icons/react` and, from this batch, `@dnd-kit/*`. Verified pre-existing: the identical `TypeError: Failed to resolve module specifier "react/jsx-runtime"` reproduces in a clean worktree at the beta.7 commit. **Not fixed here.** Adding the four map entries clears every resolution error and gets the app to its loader, but it then hangs on an esm.sh subpath that answers with HTML rather than a module, and chasing that is neither this batch's surface nor a shipped one (`examples/` is not in `package.json`'s `files`). Recorded so the next attempt starts from the import map rather than from the network. |
| 8 | pre-existing, gap not closed here | **`npm run test:regress` is not run by any workflow.** The issue for this batch requires each swap's spec to be "wired to the `test:regress` script 0.1 added", and all three are — `site/package.json`'s script globs `tests/*-regress.spec.ts`, so they are picked up. But no CI job invokes that script: `ci.yml`'s `site` job runs typecheck, build and `test:a11y` only. Every regress spec the absorption has written — batches 1, 2, 3, 6, 7 and this one's three, some 190 assertions — runs only when a human runs it. That is also why the four batch 1 / batch 3 failures recorded above went unnoticed until this batch measured them. Not wired here: the honest options are a required step, which turns CI red on those four, or a `continue-on-error` step, which is the arrangement that let them go unnoticed in the first place. Both are the cutover's call, and §5 step 1 is where the regress tiers are owed alongside the full a11y gate. |
| 8 | later PR broke this batch's own tier, fixed forward | **Tier 5 was red from the moment it armed, and the failure did not look like one.** Issue #31 moved the home page to the repo root and `git mv`'d `examples/components/` to `examples/css/` to free the directory for the generator-owned React pages. `tests/visual.spec.js` is the only surface in the repo that addresses the hand-written pages by URL, and it was not carried along: **8 of its 12 routes 404'd.** A 404 renders a viewport-sized error page, which is a perfectly stable screenshot, so all eight were reported as pixel diffs — `1280x900` against a `1280x2547` baseline — and the two genuine failures (`kitchen-sink` and `theming`, +52px from #37's sticky theme toggle) sat in the same list, indistinguishable. The paths are repointed and the baselines regenerated on Linux; the `name` keys are the snapshot filenames and were left alone, so the baselines kept their identity across the move. The fix worth keeping is the assertion: both suites now check `response.status() === 200` before capturing. The docs-app suite had already paid for this same blind spot once — the note on its `RAW_ROUTES` records seventeen baselines of a 404 page written under `--update-snapshots` and reported as success, because a 404 carries no `dark` class and so passes the light half of `expectTheme()` — and fixed the instance (`routePath()`) without closing the class. All 34 docs-app baselines regenerate byte-identical, so that half was sound. |
| 8 | pre-existing, not this batch's surface | The regenerated docs-app baselines render in a **serif fallback with a collapsed on-page TOC** — the headings are not in the charte's faces and the TOC's ordinals overlap the sidebar/content boundary. Byte-identical to the baselines committed at the start of this batch, so it is neither new nor caused by the regeneration, and both halves of the theme axis render the real app rather than an error page (verified by reading the PNGs, not by inferring it from a passing assertion). Recorded rather than fixed: tier 5's job here is to arm against what ships, and the docs-app's own font loading belongs to the site issues, not to a defect swap. Noted so that a green tier 5 is not read as a claim that these pages look right. |
| 8 | pre-existing, gap not closed here | **#31's move left 76 stale `examples/components/` references across 16 files.** `docs/components.md`'s ~40 "Live:" links now resolve to the *generated React page* for each slug rather than the hand-written CSS page they document — the wrong file rather than a dead link, so no link checker flags it — plus header comments in eight `css/components/*.css` files, four of them this batch's own (`drawer`, `kanban`, `tooltip`, and `checkbox` by way of the form fixtures). Not corrected here: the rows above this one in this file are a log and are left as written, and the live references span batches 3, 6, 7 and 8, which makes the sweep #31's fallout rather than a defect swap's diff. |
| 39 | generator defect, root-caused and fixed forward | **`?bundle` on every import-map entry gave the generated pages two copies of every shared module**, and that — not a "different mounting context" — is what [#39](https://github.com/LittleBigCode/design-system/issues/39) measured. `buildImportMap()` emitted `esm.sh/<pkg><subpath>?external=react,react-dom&bundle` per bare specifier, and `?bundle` inlines a build's dependencies: `@base-ui/react/menubar` and `@base-ui/react/menu` each carried their own copy of Base UI's internals, and `@dnd-kit/sortable` inlined its own `@dnd-kit/core`. React context is identity-based, so `Menu` never saw `Menubar`'s context (the bar stopped being one roving tab stop) and `useSortable` never saw `DndContext` (every card rendered with an empty `aria-describedby` and every drag was inert). Six of the ten assertions — menubar's arrow keys and all five kanban ones — were this one cause. Fixed by deriving `external` from the map itself, so anything else the map resolves stays a bare specifier, and by keeping `?bundle` only for a package that contributes a single entry, which is the case `@phosphor-icons/react`'s hundreds of per-icon round trips added it for. |
| 39 | docs-harness gap, fixed forward | **`.ds-speed-dial--docked` had no positioned ancestor on a generated page.** The class is documented as "scoped to a positioned ancestor rather than the viewport", and the only thing supplying one was the demo wrapper's Tailwind `relative` — which the buildless pages do not load. All four dials on `speed-dial.html` therefore docked against the viewport and stacked on the same 40px square, so the tone demo's dial intercepted the click the test aimed at the first. `.ds-example__preview` and `.ds-workbench__preview` are now `position: relative`: the panel is the demo's stage, and it is the only positioned ancestor a demo gets here. Verified to move nothing — all 12 committed visual routes render identical element rects with the property on and off. The playground also carried `absolute end-4 bottom-4`, the literal `batch1-rewiring-regress.spec.ts`'s sibling assertion exists to forbid, and now carries `ds-speed-dial--docked` like the three demos do. |
| 39 | test defect, fixed forward | **`document.styleSheets` enumerates only the sheets a `<link>` or `<style>` created**, so `batch7-rewiring-regress.spec.ts`'s four CSS scans saw nothing at all on a page that links one entrypoint `@import`ing 125 component files. Two of the four failed loudly (button's frozen modifier family, select's two `--ds-z-popover` remaps) and are #39's remaining pair; the other two — the `ds-open`/`ds-close` and `ds-agenda-event-status--` guards — are negative assertions and were **passing by finding nothing**, which is the more expensive half of the same bug. One recursive `styleRules()` helper now descends `CSSImportRule` and `CSSGroupingRule`, so all four mean the same thing whether the CSS arrives flat from a bundler or as an import chain. Passes against both `site/`'s Vite build and the generated pages. |
| 39 | reported finding not reproducible | The tenth assertion, `button-group`'s "the separator composes `.ds-separator--auto`, not a Tailwind literal", **passes at `batch/8-defect-swaps` HEAD** on the generated page — the ~145px separator the issue measured does not reproduce. Left alone rather than chased: the issue was filed against an earlier build, and the assertion is green on both harnesses. |
| 39 | same defect, second suite, fixed forward | **`tests/a11y.spec.js` carried the identical 404 defect batch 8 fixed in `tests/visual.spec.js`, and nobody looked.** #31 moved the home page to the repo root and `git mv`'d `examples/components/` to `examples/css/`; beta.8's row repointed the visual suite's 12 routes and added its `response.status() === 200` assertion, but the a11y suite addresses four of the same moved pages and was not carried along. **Four of its five routes 404'd** — `index`, `components-buttons`, `components-forms`, `components-web-components` — and axe on an error page finds no violations, so the gate reported green on four pages it never loaded. That is the same class beta.8's row named ("the fix worth keeping is the assertion") and the same class the docs-app suite paid for once before it: the instance was fixed each time, the class never was. Routes repointed, `name` keys left alone so report identity survives the move, and every navigation now goes through one `goTo()` that asserts the status. **All six pass on the real pages** — the silent green was hiding no violations, only the fact that it was silent. |
| 39 | earlier correction now stale | Beta.8's row on **`a11y: demo (signed-in console)`** ends "Not fixed here", naming `examples/demo.html`'s import map — 83 modules importing an unmapped `react/jsx-runtime`, behind it `@base-ui/react/*`, `@phosphor-icons/react` and `@dnd-kit/*`. It **was** fixed, in #31's own PR ([08061aa](https://github.com/LittleBigCode/design-system/commit/08061aa)), which is the last commit to touch that file: the map now carries 37 entries and the test passes against a console that really mounts (`.ds-console` visible is what it waits on, so it cannot pass vacuously). Recorded rather than edited into the row above, per this file's rule that the log is left as written. Noted while passing: that map is hand-maintained and has drifted off the installed versions — react 18.3.1, `@base-ui/react` 1.6.0, recharts 3.8.0, against 19.2.8 / 1.7.0 / 3.10.1 in `node_modules` — which is the drift `buildImportMap()`'s "nothing here is hand-maintained" note exists to avoid. Left to whoever owns `examples/demo.html`'s fate at the cutover. |
| 18 | pre-existing, gap not closed here | **Item 3's click timeout no longer reproduces.** The only click in `tests/a11y.spec.js` (the demo's "Sign in" button) ran clean, 6/6, in every run while landing #33. Traces to the same import-map fix batch 39's row above already dated to #31's PR `08061aa` — one cause, already recorded, closing two rows at once. |
| 33 | verified, gap not closed here | **171 of 177 moved regress + chart-marks assertions pass against the generated pages; 6 do not, none of them #33's surface.** Four are the same pre-existing autocomplete/phone-input failures batch 8's row above already named (`tests/*`, unedited, confirmed identical by name). Two are new and root-caused, filed as [#51](https://github.com/LittleBigCode/design-system/issues/51): `chart-marks.spec.ts`'s bullet-chart assertion (`examples/registry/playgrounds/bullet-chart.tsx` carries a Tailwind-only `className`, inert without Tailwind loaded, collapsing a grid track to 0px) and `kanban-regress.spec.ts`'s pointer-drag assertion (`css/base/reset.css`'s `scroll-behavior: smooth` racing the test's immediate `getBoundingClientRect()` after `scrollIntoView()`). A seventh, `drawer-regress.spec.ts`'s focus-trap-wraps assertion, flakes 0–60% run to run (missing a `settle()` after the drawer's open transition) and is noted on #51 rather than counted as a fixed number. None are `package.json`/`tests/*`-surface defects `tests/harness.ts` could fix — they are component/CSS/config bugs the harness now has the reach to see for the first time. Also filed: [#50](https://github.com/LittleBigCode/design-system/issues/50), 9 further a11y findings from the exhaustive 118-page × 2-theme sweep, none in #15 or #41's tables, allowlisted against it pending a fix. |
| 34 | executed against a standing reversal, fixed forward | **#34's retirement landed against its own reversal comment.** `direction.md` decision 1 (2026-09-04) reverses #34 outright — `site/` is not retired, it becomes the docs site at `/`, and #48 (batch 15) carries the work — and the ticket itself carries a comment saying so, dated the same day. The retirement (`e465886`, merged as `1b9a464`) executed the ticket's original pre-reversal task list anyway, three days later, deleting `site/` and `deploy/` and rewriting `pages.yml`/`ci.yml`/ADR 0002 around their absence. Reverted clean on `migration-v2` (`842e8e7`) before batch 9 started — nothing had stacked on top of the retirement yet. `site/` is restored; #34 stays open and closes when #48 lands the actual work. |
| 9 | ledger verdict voided, reversed | **`stat-card` flips after all — the css-ledger §6.1 "held" correction is voided by `direction.md`.** That correction ruled `stat-card` incumbent-holds because `StatCard.js` runs a real count-up tween the source's version lacks, and `stat-card.css` held with it. `direction.md` decision 3 and `api-swaps.md`'s 28-row table both list `StatCard` as a component whose React *does* eventually swap (batch 13, #46) — the reversal's "incoming is the model" applies here too, the count-up tween notwithstanding. `stat-card.css` is replaced from `migration-source-v1`'s `stat-card.css` in this batch; `StatCard.js` has not swapped yet (that's #46's job), so the component temporarily renders the old count-up markup against the new stylesheet until then — the same accepted, temporary mismatch every other previously-held file in this batch carries. |
| 9 | cross-boundary pin, deferred | **`stepper.css` and `vertical-nav.css` are the two files this batch does *not* flip, and neither is a "no source" case.** Both have real source counterparts (`stepper.css`, `sidebar.css`) but are pinned by a component with no swap scheduled anywhere in the remaining batch chain (#43–#49): `stepper.css` by `Wizard` (its own React swap is real, per `api-swaps.md`, but `Stepper.tsx` was already hand-fixed in batch 6 to render `stepper.css`'s *current* `.ds-stepper__marker`/`__label`/`__step` grammar specifically because `Wizard` reads it, and nothing revisits `Stepper.tsx` again to accommodate a change here); `vertical-nav.css` by `ConsoleLayout` (a permanent additive per `direction.md`/`api-swaps.md`, and batch 8 already ruled out porting `sidebar.tsx`'s full composition — only two cherry-picks land, onto `AppShell`). Unlike every other previously-held file in this batch, flipping either of these has no future batch positioned to reconcile it — the mismatch would be permanent, not transient. Left as-is; whoever eventually re-touches `Wizard` or `ConsoleLayout` inherits the call. |
| 9 | root-selector collision, deferred | **`tabs.css` is not flipped either, for a different reason than the two above: a genuine same-selector collision, not a missing future batch.** `<ds-tabs>` (`components/ds-tabs.js`) hardcodes `.ds-tabs`/`.ds-tabs__tab`/`.ds-tabpane` as its root and reads them directly. `migration-source-v1`'s `tabs.css` also declares a bare `.ds-tabs { display: flex; ... }` as ITS root (a `TabsList`/`TabsContent` composition, not a tab-button row) with a *conflicting* `display`/`flex-direction` default — unlike every other frozen-contract file in this batch (`modal`, `segmented`, `button-extras`), source's own selectors here are not free to land beside the frozen ones; the shared bare `.ds-tabs` selector would resolve by load order and silently change the web component's own root layout. Left untouched pending a real decision on how the two `.ds-tabs` roots coexist (rename one, scope by structure the way `switch.css`/`checkbox.css` already do for their own collisions) — not a call this batch should make by default. |
| 9 | ledger verdicts voided, list | **Every css-ledger `frozen` and `incumbent-holds` verdict this batch voided**, per `direction.md` decision 2 (the two exceptional cases above — `stat-card`'s and the deferred trio's — get their own rows; this is the rest). Full replacement from `migration-source-v1`, codemodded through `slot-mapping.md`: `accordion.css`, `alert.css`, `calendar-view.css` (← `calendar.css`), `code-block.css`, `color-picker.css`, `combobox.css`, `command-palette.css` (← `command.css`), `datagrid.css` (← `data-table.css`), `date-picker.css`, `date-range.css` (← `date-range-picker.css`), `date-time.css` (← `date-time-picker.css`), `file-upload.css`, `menu.css` (← `dropdown-menu.css` — `context-menu.css`/`menubar.css` already landed separately as net-new in batches 1–2 and are untouched here), `number-input.css` (← `number-field.css`), `popover.css`, `rating.css`, `skeleton.css`, `tag-input.css` (← `tags-input.css`), `time-picker.css`, `toast.css`, `tree.css`, `wizard.css`, and `form-controls.css`'s radio block (← `radio-group.css` — the only file the css-ledger's one `split` verdict covered). Source content merged into the existing frozen classnames, not a file swap (external web-component readers, so the swap risk this batch otherwise accepts does not apply): `segmented.css` (← `toggle.css` + `toggle-group.css`, appended clean — no selector collision), `modal.css` (← `dialog.css` + `alert-dialog.css`, appended clean plus two refinements folded onto the existing frozen selectors), `button-extras.css` (← `split-button.css`'s `.ds-split-button-caret*` slice only, appended clean — `ButtonGroup`/`IconButton`'s own classes are untouched, per `direction.md`'s genuine-additive call on `ButtonExtras`). |
| 11 | plan question answered | **The `class-variance-authority` question in decision 4's dependency table resolves to: don't add it.** `react/lib/variants.ts` already gives every absorbed component's `cva(base, spec)` call the same resolver — a variant map to class names, nothing else — in ten dependency-free lines, and it exists for exactly this reason (its own header comment cites the resolution, though it misattributes it to ADR 0001, which is about Base UI, not `cva`; no ADR currently records this call). `tabs`, `toggle-group`, `stat-card` and `sidebar` resolve their variants through `variants()` when batches 12–14 land them. |
| 11 | dependency withheld, decision 13 reopened | **`@shadcn/react` is not added.** Installing it under React 18 (`npm install react@18 react-dom@18 @shadcn/react`) only warns rather than hard-fails — its `react`/`@types/react` peers are `>=19` but marked `peerDependenciesMeta.optional`, so npm's resolver doesn't block — but its own `devDependencies` pin `react`/`react-dom@19.2.3` and nothing in its test matrix touches 18, so "optional" here reads as "don't require React at all," not "works on 18." Adding it would keep the `>=18` peer range technically installable while shipping a component (`message-scroller`) nothing has verified there. This is `react-ledger.md`'s original **admit CSS-only** verdict for `message-scroller` (#156, "narrow dep buys nothing") holding up under a fresh check, not `direction.md` decision 4 reversing it as intended for the other three previously-admit-CSS-only components (`carousel`, `input-otp`, `resizable` — all confirmed 18-compatible and added). `message-scroller` stays CSS-only; decision 4's dependency table and decision 13 both need a line acknowledging the one dependency that didn't clear the bar. |
| 11 | pre-existing, discovered here | **The React-18 CI job decision 13/batch 11 asked for already exists** — `ci.yml`'s `package` job installs the tarball with `react@18`/`react-dom@18` and typechecks a consumer snippet against it ("Consumer typechecks against the tarball on React 18"). Nothing new added for that checklist item; the one gap this batch closes is narrower — a tarball-content assertion for `dist/react/index.js` and `css/diametral.css`, which did not exist. |
| 12 | plan measurement wrong, fixed forward | **`@tanstack/react-table` was pinned to `^9.2.4` in batch 11 — wrong major.** `source/data-table.tsx` imports `getCoreRowModel`/`useReactTable`/`ColumnDef` — the v8 API; v9 replaced it with a `TableFeatures` generic system (`createCoreRowModel`, `ReactTable`) that doesn't export any of those names. Repinned to `^8.21.3` (latest v8, `react >=16.8`) here in batch 12, once landing `data-table.tsx` actually exercised the dependency batch 11 only added and typechecked against a snippet that never imported it. |
| 12 | dependency added, decision 4 incomplete | **`react-day-picker` is a new dependency decision 4's table never listed.** `calendar.tsx` (needed by `date-picker`/`date-range-picker`/`date-time-picker`, none of them net-new) imports it; nothing in batch 11's six-dependency bill covered it. Verified React-18-compatible (`react: >=16.8.0` peer, real not optional) and added to `dependencies`. Decision 4's table is incomplete, not wrong — it enumerated batch 12's own net-new components' deps, not what batch 12's non-net-new components transitively need. |
| 12 | plan question answered | **`tabs`, `toggle-group`, `stat-card` and `sidebar`'s `class-variance-authority` calls resolve the same way batch 11 already ruled for the dependency question: convert to `react/lib/variants.ts`, add no dependency.** Each is a mechanical `cva(base, spec)` → `variants(base, spec)` swap; the one thing `variants()` has no equivalent for is cva's `VariantProps<typeof x>` helper type, so each caller hand-writes the small union type instead (matching every `variants()` caller already in the tree — none use a `VariantProps`-shaped helper either). `react/lib/cx.ts` gained a second export, `WithClassName<T>`, for a different, unrelated gap this surfaced: several Base UI parts (`Dialog`/`AlertDialog`/`Menu`/`NumberField`/`Radio`/`Toast`/`Tabs` backdrops, popups and titles) type `className` as `string \| ((state) => string \| undefined)`, and `cx` only ever joins strings — every absorbed wrapper that re-derives its own `className` narrows the prop back to `string` with `WithClassName<T>` rather than silently mishandling a function at runtime. |
| 12 | blocked, deferred to batch 13 | **6 of #45's 26 don't land: `code-block`, `date-picker`, `date-range-picker`, `date-time-picker`, `time-picker`, `multi-select`.** Each transitively imports `./calendar.js`, `./icon.js` or `./combobox.js` — decomposed source modules that don't exist under those exact names yet, only as the PascalCase incumbents `Calendar.tsx`/`Icon.tsx`/`Combobox.tsx`. Two independent blockers, not one: (1) TypeScript's cross-case-collision check (TS1149) refuses a same-spelled lowercase sibling next to either of the first two in the same `include`, on every OS — not a macOS-only quirk, since the check is about import-specifier casing, not the filesystem (confirmed: it fires under Linux too). A first attempt at landing `icon.tsx`/`calendar.tsx` anyway actually clobbered `Icon.tsx`/`Calendar.tsx` in place on this checkout's case-insensitive filesystem before the collision was caught by `tsc`; both were restored from git (`git checkout --`) with no lasting damage, recorded here as the reason landing them "alongside" isn't an option the way it is for every other name collision this batch handles via `./react/*` subpaths — there's no file path left to put the subpath version at. (2) `combobox.js`'s decomposed parts (`ComboboxChip`, `ComboboxChips`, …) that `multi-select.tsx` needs don't exist on the incumbent `Combobox.tsx` at all, decomposition or not. All six are batch 13 (#46) prerequisites: `Icon`, `Calendar` and `Combobox` are three of its own 28 swaps, and once any one of them lands for real (incumbent gone, decomposed source in its place, one name, no collision), its dependents here become a straight port — the ported-but-blocked source, the exact `cn`→`cx` and hook-path rewrites, and the `WithClassName` sites are already worked out per-file in this batch's own history and don't need re-deriving. |
| 12 | pre-existing, compounded | **`tabs.css`'s root-selector collision (batch 9's corrections row, still unresolved) now has a real consumer.** Landing `react/components/tabs.tsx` doesn't make that call — still deferred, still "not this batch's decision by default" — but it does mean `check-contracts.mjs`'s class-resolution baseline moves from **322 to 326**: `tabs.tsx`'s `ds-tabs-trigger`/`ds-tabs-content` resolve to no selector, counted once under `react/` and once under `dist/react/`, same accepted-mismatch shape as batch 9's `stat-card` case. `dialog`/`alert-dialog`/`sidebar` land with real, matching CSS (see next row) and add zero new failures. |
| 12 | css landed alongside the react it renders | **`modal.css` gets `dialog.css` + `alert-dialog.css` appended** (own additive `ds-dialog-*`/`ds-alert-dialog-*` classes, the frozen `ds-modal__*` grammar untouched — batch 9 skipped this specifically for lack of a consumer, see its own header comment) **and `sidebar.css` lands net-new** (674 lines, wired into `diametral.css`'s import graph). `sidebar.css` also needed 8 `--sidebar-*` role tokens (`--sidebar`, `--sidebar-foreground`, …) that `migration-source-v1`'s own `globals.css` defines but this package never ships — defined once in a `:root` block at the top of the file, values copied straight from that source (`--sidebar: var(--ds-bg-alt)`, …), since `slot-mapping.json` deliberately leaves the whole `--sidebar-*` family component-local rather than inlining it batch-9-style. |
| 12 | plan item not actionable yet | **The a11y-spec checklist item for `dialog`/`alert-dialog`/`command`/`resizable` has no route to test against.** Both a11y suites (`tests/a11y.spec.js`, `site/tests/a11y.spec.ts`) are route-driven — static `examples/` pages for the web-component layer, `site/`'s docs registry for the React layer — and neither has an entry for these components yet; that's batch 16's (#49) dual React/HTML example machinery, not batch 12's. No isolated component-level a11y harness exists in this repo to substitute. Deferred to whichever batch first gives these a route: likely #49, possibly sooner if #46 wires a docs page while swapping their incumbents. |
| 13 | pre-existing, unrelated, not fixed here | **`test:a11y`'s remaining two failures (`bubble` dark, `attachment` dark, both color-contrast) touch components last changed in batches 2/3/7** — nothing this batch's commits reach. Left alone, same treatment as `test:regress`'s unrelated failures above. |
| 13 | pre-existing, environmental, not fixed here | **Every one of `test:visual`'s 17 routes fails in this environment, not just the four this batch changed** — `button`/`badge`/`card`/`item` (untouched by any recent batch) show the same shape of failure as `calendar`/`data-table`/`dropdown-menu`/`sidebar`: a small, consistent full-page height mismatch (tens of px) and a 3–5% pixel-diff ratio, the signature of a font-rendering or docs-chrome difference between wherever the committed baselines were captured and this environment, not a real regression in any of them. Left alone for the routes this batch didn't touch — re-baselining them would paper over whatever the real difference is, which is a job for whoever can compare against the environment the baselines were captured in. The four this batch actually changed (`calendar`, `data-table`, `dropdown-menu`, `sidebar` — real new markup, not environmental drift) are re-baselined here, scoped with `--grep` so nothing else moved. |
| 13 | pre-existing, unrelated, not fixed here | **9 of `test:regress`'s 15 failures on this batch's run touch no file this batch changed.** `batch1-rewiring-regress.spec.ts` (autocomplete's clear button, 1), `batch3-rewiring-regress.spec.ts`'s phone-input and toggle rows (4), `color-picker-regress.spec.ts` (the swatch-group keyboard model, 4) and `drawer-regress.spec.ts` (the focus-trap-wraps assertion, 1) all fail against `autocomplete.tsx`/`phone-input.tsx`/`toggle.tsx`/`color-picker.tsx`/`drawer.tsx` — none touched by any commit in this batch (checked: `git log --name-only` across every batch-13 commit has no match for any of the five). The phone-input/autocomplete pair and the drawer flake are the same ones batch 33's row already named; `color-picker-regress`'s four are new to this file but equally outside `api-swaps.md`'s 28 rows (`ColorPicker` is the one fully-compatible row — no prop or behaviour change at all). Left alone. The other 6 failures — `calendar-regress`'s arrow-key assertions and `radio-group-regress`'s name assertion — are this batch's own and are fixed above. |
| 13 | pre-existing regression, found running the release-blocking gate, not fixed here | **`check-contracts.mjs`'s class-resolution check — release-blocking per `ci.yml`'s own comment — currently fails 20 ways, all pre-existing.** `tabs.tsx`'s two (`ds-tabs-trigger`/`ds-tabs-content`) are the already-known, already-recorded root-selector collision (batch 9's row, measured into the baseline by batch 12). Newly found here: `menubar.tsx` (6 occurrences: `ds-menu__item`, `ds-menu__item--danger`, `ds-menu__header`, `ds-menu__divider`) and `theme-switcher.tsx` (1: `ds-menu__item`) resolve to no selector at all, in both `react/` and `dist/react/` (10 total, ×2 = 20 with `tabs.tsx`'s 2×2). Root cause: batch 9's wholesale replacement of `css/components/menu.css` (`← dropdown-menu.css`, its own corrections.md row) overwrote the file's entire previous content — including the `.ds-menu__item`/`.ds-menu__header`/`.ds-menu__divider` rules `menubar.tsx`/`theme-switcher.tsx` (net-new, batches 1–2, sharing the incumbent's menu vocabulary on purpose) depend on — with source's `.ds-dropdown-menu-*` classes instead. Neither `menubar.css` nor `theme-switcher.css` (checked directly) define the old classes either; only comments in `menubar.css` still name them. This has been orphaned since batch 9, three batches before this one, and the gate was never actually run to catch it — same root cause as this batch's `site/` `node_modules` finding above: the tooling exists, nobody was exercising it for real. Not fixed here: reconciling `.ds-menu` vocabulary for menubar/theme-switcher against `menu.css`'s new content is a real design call (restore the old rules alongside the new ones? move menubar/theme-switcher onto `.ds-dropdown-menu-*`?) outside api-swaps.md's 28 rows and this batch's remit. |
| 13 | environment gap found and fixed, surfaced a real pre-existing one | **`site/`'s `node_modules` had never been installed in this environment — every prior batch's "site typecheck clean" was `tsc --noEmit` against `site/tsconfig.json`'s solution file (`files: []` + `references`, no `-b`), which validates nothing without `-b` and gave a false-clean signal all session.** Running `npm install` in `site/` (fast, from cache) made `tsc -b` and `vite build` real for the first time. `tsc -b` then passed clean (batch 13's own call sites, including `site/src/main.tsx`/`docs-search.tsx`, are genuinely sound). `vite build` surfaced the first real finding: `plugins/demo-source.ts`'s anatomy-coverage check — which throws if a landed component's demo directory exists but doesn't exercise all its exported parts — failing on 68 parts across 13 slugs. 33 across 7 slugs were batch 13's own gap (`alert`, `combobox`, `command`, `dropdown-menu`, `number-field`, `popover`, `sidebar` — this batch's first real demo for each, incomplete); fixed by extending those demos to exercise every part. The other 35 across `dialog`/`alert-dialog`/`carousel`/`input-otp`/`resizable`/`tabs`/`toggle-group` are unrelated to the 28 swaps: each slug's registry *page* documents a different genuine additive that happens to share the name (`Modal` for "dialog", the inline `Tabs` for "tabs", …), and the real component (landed batch 12, #45) has no route of its own — batch 16's (#49) dual React/HTML example machinery, exactly as this file's batch-12 row already said, just never actually checked because the build had never run. Added to `ANATOMY_EXCEPTIONS` with a batch-16 pointer rather than written here — giving each a demo means giving it a route, which is a slug/routing decision, not a few lines of JSX. |
| 13 | pre-existing gap, not fixed here | **`CHANGELOG.md` has no entry for batches 9–12** — it jumps from `1.0.0-beta.8` (2026-09-03) straight to this batch's `1.0.0-beta.13` entry. Each of those four batches shipped real, consumer-visible change (batch 9's wholesale CSS replacement across ~27 files, batch 10's `.ds-*` utility layer, batch 11's five acquired dependencies, batch 12's 19 landed components) with no changelog row. Not backfilled here — out of this batch's scope, and guessing at what each batch's own entry would have said risks getting it wrong where the batch's own commit/corrections history should speak for itself. Flagged for whoever compiles the real 1.0.0 release notes. |
| 13 | plan measurement gap, not fixed here | **`react/index.tsx`'s own header comment claimed Tabs/Segmented/Modal were batch 13's (#46) to swap; `api-swaps.md`'s 28-row table — the batch's own source of truth — does not list `Tabs`, `Segmented`/`ToggleGroup` or `Modal`/`Dialog`/`AlertDialog` at all.** Treated `api-swaps.md` as authoritative rather than the barrel comment: this batch swaps exactly its 28 rows and leaves the inline `Tabs`/`Segmented`/`Modal` (genuine additives, no incoming counterpart, per this same file's corrected comment) and incoming's landed-additive `tabs.tsx`/`toggle-group.tsx`/`dialog.tsx`/`alert-dialog.tsx` untouched. Whether these three are ever meant to swap, and if so in which batch, isn't settled by anything this batch read — a real gap for whoever plans the next one, not something #46 should decide by default. |
| 13 | own finding retracted, real cause was a stale demo, fixed forward | **Retraction of this file's own prior row calling `Checkbox`/`Switch` "discarding children" a component bug.** Both wrappers' own comments say plainly that batch 7 (1.0.0-beta.7) made the change on purpose: the root became the box/track itself rather than a `<label>` wrapping one, so the caller-written association moved from `children` text to an explicit `Label htmlFor` — the exact pattern `RadioGroupItem` already uses. `label/with-controls.tsx`'s `<Checkbox>I accept the charter</Checkbox>`/`<Switch>Send me release notes</Switch>` (and the page's own comment claiming they self-label) were never updated off the pre-batch-7 API, which is why the text renders nowhere and `site/tests/a11y.spec.ts`'s `label` page failed — not a component defect, a demo six batches out of date, caught only now because the a11y suite had never really run in this environment. Fixed forward: both rows now pass an explicit `id` to the control and a `Label htmlFor` beside it, and the page's own comment corrected. Verified live (dev server, not just the built preview): the checkbox's `role="checkbox"` element now carries a real `aria-labelledby`, resolved via Base UI's own `.labels`-API fallback onto the sibling `Label`. |
| 13 | pre-existing, unrelated, not fixed here | **The same `label` page's `Slider` row (`id="label-seats"`, unedited by this batch) is a real, different, pre-existing gap: the `id` passed to `Slider` lands on its outer wrapper, not the native `<input type="range">` inside it, so the sibling `Label htmlFor="label-seats"` doesn't reach the actual control axe checks.** Confirmed live: `#base-ui-_r_d_` (axe's remaining node after the `Checkbox`/`Switch` fix above) is that inner range input, auto-generated id, no label association at all — unlike `Checkbox`/`RadioGroupItem`, `Slider` apparently has no equivalent fallback wiring. Not an api-swaps.md row, not touched by any batch-13 commit. Left for whoever owns `slider.tsx`. |
| 13 | pre-existing CSS defect, fixed forward | **`code-block.css`'s `.ds-code-block` panel references `var(--ds-black)`, a token that doesn't exist anywhere in `css/tokens.css` (checked: zero matches for `--ds-black:`).** An undefined custom property makes the declaration invalid, so the "dark, flat panel" this component is documented as had no background or border at all — in light theme it showed through as the page's own light background, and `.ds-code-block-filename`'s `#8e9099` grey (designed for a dark panel) failed contrast against it (caught by `site/tests/a11y.spec.ts`, light theme only — dark theme's own background happened to be close enough to pass anyway). Not in source at all (source ships no `code-block.css`; this file is batch 9's own construction) and not from this batch's own edits (only the React binding moved this batch; last CSS touch was batch 9). Fixed forward: `--ds-black` → `--ds-noir` (`#161616`, this package's actual darkest token) on both the border and the background — ~4.98:1 against the filename grey. |
| 13 | pre-existing CSS defect, surfaced by this batch, fixed forward | **`calendar-view.css`'s selected/range-end/range-start day rules paired `--ds-accent` (`--ds-rouge`, `#ff2a00`) background with `--ds-on-accent` (`--ds-blanc`, pure white) text — 3.78:1, below the 4.5:1 normal-text threshold WCAG (and axe) require.** This pairing was batch 9's wholesale CSS replacement (before this batch), not something this batch wrote, but nothing rendered a real selected day against it until this batch's Calendar swap actually composed `data-selected-single`/`data-range-*` for the first time — caught by `site/tests/a11y.spec.ts` on both themes now that it does. `Button`'s own `tone-red` variant already routes around the identical problem with `--ds-noir` (near-black) instead of white on the same red; matched here — `color: var(--ds-noir)` on all three rules, ~4.49:1, at parity with the existing accessible choice elsewhere in the same package rather than a new one. |
| 13 | source defect, fixed forward | **Source's own `calendar.tsx` (`feat/css-conversion` @ `b97c651`, unmodified by the port) never attaches its `CalendarDayButton`'s `ref` to the rendered `Button`** — `const ref = React.useRef(...)` and the `useEffect` that calls `ref.current?.focus()` when `modifiers.focused` flips both exist, but the JSX passes every other prop except `ref` itself, so `ref.current` is always `null` and arrow-key navigation across the day grid never moves DOM focus (react-day-picker's own `moveFocus` correctly updates *which* day should get it; nothing carries that through to the actual element). Caught by `site/tests/calendar-regress.spec.ts`'s `ArrowRight`/`ArrowDown` assertions on `test:regress` — every other assertion in that spec passed, since they don't require focus to actually move. Fixed forward: `ref={ref}` added to `CalendarDayButton`'s `<Button>`. |
| 13 | source defect, fixed forward | **`radio-group.tsx`'s `RadioGroup` (landed batch 12, #45, wired into the barrel this batch) has no default `name` — Base UI's own `name` prop has none — so every `RadioGroupItem` inside rendered `name=""`, which is not one native group of anything.** The incumbent `RadioGroup` defaulted it to a generated id ("best left alone", per its own registry docs) and every radio-group demo assumed the swap kept that. Caught by `site/tests/radio-group-regress.spec.ts`'s `test:regress` run, not by typecheck or the anatomy-coverage build. Fixed forward: `RadioGroup` now falls back to `React.useId()` when the caller doesn't pass `name`. |
| 13 | cross-component contract broken by the swap, fixed forward | **`label.css`'s sibling rule still traded Label's field-heading type for sentence case beside `[data-slot="radio-group-item"]`, after `Radio`→`RadioGroupItem` stopped self-labelling.** Batch 7 wrote that rule (`radio-group-item`/`checkbox`/`switch` are each a `<label>` around their own text, so a following Label is the row's prose) against the *incumbent* `Radio`, which drew its own text as a child the same way `Checkbox`/`Switch` still do. The incoming `RadioGroupItem` (this batch's swap) draws only the dot — the label is composed separately, same shape as `Select`/`Slider`/`Textarea` — so the sentence-case override was firing on a control that no longer earns it, which would have shown every `radio-group`/`label` demo's field caption in the wrong voice. Found reading the `label` registry entry's own prose while updating it for the swap, not from a test — no regress spec covered this selector. Fixed forward: `radio-group-item` dropped from `label.css`'s sibling selector list. |
| 13 | prediction proved wrong, fixed forward | **Batch 9's row on `stepper.css`/`vertical-nav.css` said "nothing revisits `Stepper.tsx` again" for `Wizard`'s sake — batch 13 does, because `Wizard`'s own swap (`api-swaps.md`: compatible) is real.** Source's `wizard.tsx` composes a `StepperSeparator` between items that `react/components/stepper.tsx` doesn't export — deliberately, per that file's own note: the connector is a `::after` on `.ds-stepper__step`, drawn by the held `stepper.css`, so a separator element would be inert markup that also breaks the row's `flex: 1 1 0` per-step sizing by inserting a non-`<li>` sibling into the `<ol>`. Fixed forward by omission: the ported `wizard.tsx` composes `Stepper`/`StepperItem`/`StepperIndicator`/`StepperContent`/`StepperTitle` and drops `StepperSeparator` entirely rather than adding a part with nothing to render. `stepper.css`/`Stepper.tsx` stay exactly as batch 9 left them. |
| 13 | capability loss, accepted | **`Tree`'s swap drops the same class of thing `DataGrid`'s did: a custom keyboard-navigable tree with roving tabindex and arrow-key expand/collapse.** `site/tests/tree-regress.spec.ts` guarded `.ds-tree__label`/`.ds-tree__toggle` (0.11's classes, already gone — batch 9 replaced `tree.css` wholesale) with a single tab stop and Arrow/Home/End navigation; the source's `tree.tsx` is `CollapsiblePrimitive.Root`s with no roving-tabindex layer at all — Tab walks every trigger, and there is no arrow-key handling. Deleted rather than weakened, same reasoning as the `DataTable` row below: a real accessible-tree keyboard layer is a feature request, not this batch's job. |
| 13 | capability loss, accepted | **`DataGrid`→`DataTable` drops the custom keyboard-navigable grid entirely — no `role="grid"`/`role="gridcell"`, no roving tabindex, no arrow-key cell navigation, no inline-editor Escape/Enter handling.** `site/tests/data-table-regress.spec.ts` existed specifically to guard that model (its own header names the two defects it once caught: no grid roles, no roving focus) against `.ds-datagrid__table`/`.ds-datagrid__td--editable`/`.ds-datagrid__edit`, none of which the source's `data-table.tsx` renders — it's a plain `<table>` (`table.tsx`'s `Table`/`TableHeader`/`TableBody`/`TableRow`/`TableCell`) with no ARIA grid layer and no keyboard model beyond the browser's native tab order. This is "incoming is the model" applied to an axis the source genuinely doesn't cover, not a defect introduced by the port. The spec is deleted rather than weakened to a vacuous pass; a real accessible-grid keyboard layer over `@tanstack/react-table` is a feature request, not this batch's job, and whoever picks it up should know the bar existed once. |
| 16 | own regress suite went stale, fixed forward | **Giving `carousel`/`input-otp` a real binding (part 2) broke 6 of `batch3-rewiring-regress.spec.ts`'s own assertions — all against the exact contract this batch changed on purpose.** Both `"…ships CSS with no binding"` describe blocks asserted the *absence* of an import line, which is now false; `input-otp`'s slot-count/`aria-hidden` assertion assumed the old stub's 2-group, 3-3 split (the real binding's `basic.tsx` demo is one ungrouped run of 6, per its own registry copy — the split lives in `with-separator.tsx` instead) and its `data-active` assertion assumed a slot was statically marked active without any real focus (the real input only sets it on actual `:focus`). `carousel`'s `--third`-modifier assertion failed for a different reason: the ported `multiple.tsx` demo used the source's literal `basis-1/3` Tailwind class, when `carousel.css`'s own header comment already says the dedupe exception resolves onto a `--third`/`--half` *class* — fixed in the demo file, not the test. Also surfaced fixing this: `carousel.tsx`'s real `CarouselPrevious`/`CarouselNext` rendered their accessible name in an `sr-only` span, a utility this package has never shipped (checked: zero matches for `.sr-only` anywhere in `css/`) — the name was rendering as plain visible text next to the icon. Fixed forward: swapped both to `IconButton` with a `label` default, matching what `carousel.css`'s own comment already said the contract should be. Both describe blocks and all their assertions rewritten to match the real binding; all pass now (`npx playwright test tests/batch3-rewiring-regress.spec.ts`: 35/39, the other 4 pre-existing, see next row). |
| 16 | pre-existing CSS defect, surfaced by this batch, fixed forward | **`alert.css`'s `.ds-alert--destructive` read `color: var(--ds-danger)` — the brand red itself, constant across themes — for its title/description text, rather than `--ds-danger-ink`, the ink token `.ds-alert--tone-danger` right below it in the same file already uses for exactly this.** `--ds-danger` on the dark theme's `--ds-surface` measures ~3.3:1, below the 4.5:1 normal-text threshold; `--ds-danger-ink` is themed separately (`#f08a7e` in dark) and clears it. Not a batch-16 defect — `alert.css` predates this batch — but nothing exercised the `destructive` variant (as opposed to the five `tone-*` ones) until this batch's first `alert/*` demos to use it (`validation-summary`, `with-link`), caught by the full `test:a11y` run. Fixed forward: `.ds-alert--destructive`'s `color` and `--tone-ink` both switched to `--ds-danger-ink`. **Real gap found chasing why the fix didn't show up live:** `site/src/main.tsx` imports `@diametral/design-system/dist/diametral.css` — the flattened root build output, not `css/diametral.css` directly — so a root `css/` edit needs `npm run build:css` (or the full root `npm run build`) at the repo root before `site/`'s own build picks it up; editing `css/` alone and rebuilding only `site/` silently keeps serving whatever `dist/` last had. Not documented anywhere obvious; worth a line in the root README or CLAUDE.md for the next batch that touches CSS after `site/` already exists. |
| 16 | pre-existing, unrelated, not fixed here | **`batch3-rewiring-regress.spec.ts`'s `"focus is a 2px outline, not a colour ring in box-shadow"` (toggle) fails independently of anything this batch touched** — `toggle.tsx`/`toggle.css` are untouched by any batch-16 commit. A scripted `.focus()` call doesn't reliably trigger `:focus-visible` the way a real Tab keypress does in Chromium, which is the likelier cause than a real CSS regression, but not verified further here — left for whoever owns `toggle.tsx`. |
| 16 | issue closed, root cause retired | **#39 ("10 regress assertions pass on `site/`'s docs app but fail on the generated component pages") needs no fix here — its root cause no longer exists.** `examples/` (the generated buildless pages) is deleted per direction.md decision 6; `scripts/build-docs.mjs`, which generated it, is still present but wired into no `package.json` script — dead code, not touched here (surfacing it, not this batch's to remove). Re-ran the exact suite #39 named (`kanban-regress.spec.ts`, `batch1-rewiring-regress.spec.ts`, `batch3-rewiring-regress.spec.ts`, `batch7-rewiring-regress.spec.ts`) against `site/`'s own build: all 10 of #39's originally-failing assertions pass (menubar arrow-key travel, speed-dial escape, button-group separator width, button modifier resolution, select popover z-index, all 5 kanban keyboard/drag cases). `site/` is now the only docs surface, so there is no second "generated page" left for its markup to diverge from. |
| 16 | plan measurement wrong, fixed forward | **Issue #49's own "67 interactive / 12 `<ds-*>` elements" both measure one high.** Grepping `react/components/*.tsx` for a Base UI import or the component's own `useState` — the two signals the issue text itself names — finds 63 of 121 files, not 67; `components/*.js`'s `customElements.define(...)` calls name 11 real tags, not 12 (`index.js` is a barrel, not a twelfth element). Treated the measured counts as authoritative for `registry.ts`'s `wiring` field (the per-component "what JS to wire" note, decision 9) rather than inventing a 67th component or a 12th element to match the plan's own arithmetic. |
| 16 | plan item finished, wiring-notes gap closed | **Part 4's wiring-notes pass (46 of the 63 measured interactive components) left 11 real gaps, not the 17 a naive re-measure first suggested.** Re-diffing `registry.ts`'s `wiring`-less slugs against the interactive list found `alert-dialog`, `dialog`, `tabs` and `toggle-group` already covered — under `alert-dialog-primitive`/`dialog-primitive`/`tabs-primitive`/`toggle-group-primitive`, the four batch-12-orphan slugs that exist specifically because these components' names collide with an existing additive (this file's batch-13 and batch-9 rows); the bare slugs are the *additives* (`Modal`, the inline `Tabs`, `Segmented`), which are presentational and correctly have none. `field-array` was the other false positive — no Base UI import, no `useState`, genuinely presentational chrome per its own header comment ("no state, no opinion about what an entry contains"). Added `wiring` to the 11 that were real gaps: `split-button`, `wizard`, `calendar`, `combobox`, `date-picker`, `date-range-picker`, `multi-select`, `input-otp`, `phone-input`, `command`, `resizable` — bringing the field to 57 of 123 registry entries. `tabs-primitive`'s existing note already names the `.ds-tabs` root-selector collision (batch 9's row) as deliberately unresolved; re-confirmed that's still the right call (three prior batches independently declined to make it, and it isn't in #49's own task list) rather than fixing it in passing — a CSS change was drafted, then reverted once the wiring note surfaced it was already a known, intentional gap, not an oversight. |
| 52 | plan measurement stale, fixed forward | **Issue #52's own 322-across-25-components count is a stale pre-batch-16 measurement — batch 16 (#49) already swapped 22 of those 25 onto a real incoming component, leaving 20 failures across exactly 3 (`menubar.tsx`, `tabs.tsx`, `theme-switcher.tsx`), confirmed by running `check-contracts.mjs` fresh rather than trusting the issue body.** Both were this file's own open questions (batch 13's and batch 9's rows) resolved by `direction.md`'s reversal: `menubar.tsx`/`theme-switcher.tsx` now wear the landed `dropdown-menu.tsx`'s `.ds-dropdown-menu-*` classes (already fully defined in `menu.css`) instead of the pre-#42 incumbent's orphaned `.ds-menu`/`.ds-menu__item` vocabulary — no CSS change needed, only the two `.tsx` files' classnames. `tabs.css`'s root-selector collision is resolved the way `switch.css`/`checkbox.css` tell their own collisions apart: `.ds-tabs:has(> .ds-tabs-list)` (incoming's React grammar) vs `.ds-tabs:not(:has(> .ds-tabs-list))` (the `<ds-tabs>` web component's), with incoming's real `tabs-list`/`tabs-trigger`/`tabs-content` rules added via `slot-mapping.json`'s codemod, `.dark` overrides dropped as redundant (every color they set already resolves through `--ds-ink`/`--ds-bg`/`--ds-rule`, themed at the token layer). `check-contracts.mjs`: 0 failures, no new ones. |
| 51 | one of two findings fixed, one root cause retired (same shape as #39) | **[#51](https://github.com/LittleBigCode/design-system/issues/51)'s kanban finding is real and is fixed: root's `playwright.config.js` now sets `use.contextOptions.reducedMotion: "reduce"`, matching `site/playwright.config.ts`'s own setting (and comment) exactly — `tests/chart-marks.spec.ts`'s per-file opt-out (`test.use({ contextOptions: { reducedMotion: "no-preference" } })`, already present) needed no change. The bullet-chart finding's root cause no longer exists: it was filed against `examples/registry/playgrounds/bullet-chart.tsx`, three commits before batch 47 (`4580006`, the same day) moved that file to `site/src/registry/playgrounds/` and retired the buildless `examples/components/*.html` generator #51 blamed — the exact resolution batch 16's own row above records for #39. `bullet-chart.tsx`'s `className="w-full max-w-md"` is not a leftover: every one of its ~30 sibling playgrounds sizes itself the same Tailwind way (`card.tsx`, `panel.tsx`, `collapsible.tsx`, `item.tsx`, …), because `site/`'s Vite build — the file's only remaining reader — loads Tailwind for real. Stripping it to match #51's literal ask would regress the one surface that renders it, for a class of page (`examples/`) `check-contracts.mjs`'s `root-is-package-only` assertion (batch 14's own row) now hard-fails if it reappears. Root's own `tests/harness.ts`, `tests/chart-marks.spec.ts` and `tests/kanban-regress.spec.ts` still assume that directory exists (`readdirSync` at module scope) and so still crash at collection (`ENOENT`) independent of both findings — the same gap batch 14's row 23 recorded and batch 16's row 127 left unresolved for #39; not this issue's to close either. `npm run test:regress` could not be re-run end to end for that reason, compounded here by this environment's port 8080 being held by an unrelated pre-existing process (also already recorded, batch 14's row 23) — same as every prior attempt in this environment. |
