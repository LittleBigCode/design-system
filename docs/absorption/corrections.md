# Corrections found during execution

Not a derived artifact. The four files beside this one are **copies** carrying a
provenance header, and correcting a measurement in a copy diverges it from the
source silently. This file is where a batch records what it found instead: a
source defect it fixed forward, or a measurement in the plan that execution
proved wrong.

The rule it serves is [`batch-plan.md`](./batch-plan.md) §"The source ref":
nothing unfreezes the pin, so a defect is fixed forward in the target, inside
the batch that hits it, and recorded here as one line. The accepted cost is that
source and target diverge with no trace outside this file.

One row per finding. `batch` is the batch that found it, not the one that owns
the surface.

| batch | kind | finding |
| --- | --- | --- |
| 4 | source defect, fixed forward | `sparkline.tsx` drew a flat series along the floor of its box rather than through the middle: every point equal makes the range zero, and the normalisation divided by it. Fixed in the absorbed component. |
| 4 | source defect, fixed forward | A focus ring that drew nothing — `outline: 2px solid transparent`, Tailwind's `outline-hidden` idiom — was bound to `:focus-visible` with the charte's real 2px outline. |
| 5 | plan measurement wrong | The dedupe-exception table gives batch 5 **5** exceptions across `funnel-chart`, `radar-chart`, `scatter-chart`, `treemap` and `waterfall-chart`. `bullet-chart.css` carries a sixth, in the same form and for the same reason (`[--bullet-label:7rem]` / `[--bullet-value:4rem]` kept literal so `tailwind-merge` could dedupe `in-table.tsx`'s override). The real count is **6**, and the plan's 36-owed total is **37**. All six are resolved in beta.5 — the rule is that an exception is never carried across, and the count is not what decides that. |
| 5 | source comment wrong, no fix needed | `funnel-chart.tsx` carries a comment saying recharts gates a funnel's labels behind `showLabels = !isAnimating`, that the animation therefore restarts forever, and that "with the animation off they render on the first paint" — and then sets no `isAnimationActive` anywhere in the source. Against the recharts this package pins, the labels render with animation on, so the component is absorbed unchanged and `site/tests/chart-marks.spec.ts` asserts the labels are present under animation, which is the one gate that opts back out of reduced motion. |
| 6 | plan question answered | The "one look owed" on `agenda` and `pagination` (batch-plan §4 batch 6, CSS ledger §6.2). **`agenda` is a strict superset** — same `events`/`emptyMessage` props and the same grouping and sorting, plus a `locale` the incumbent could not have (it hardcoded English weekday and month tables), plus `<section>`/`<h3>`/`<time datetime>` where the incumbent emitted a flat run of divs. **`pagination` is not a superset in shape but loses no capability**: the incumbent's `page`/`pageCount`/`onChange` disappear, but its window logic ships intact as the exported pure function `paginationRange()`, and the parts buy a page that can be a real `<a href>` — crawlable, middle-clickable — which the incumbent's buttons could not be. That is composition replacing configuration, which is the trade every applier in this migration makes, not a regression like #164's `time-picker`. Both land; both stylesheets replace. The recipe for rebuilding the controlled pager is in `docs/migration/from-0.11.md`. |
| 6 | plan item resolved early | The `kbd` / `input-group` cascade inversion (batch-plan §4 batch 6, source [#171](https://github.com/diamorval/design-system-diametral/issues/171)) is **fixed now rather than shipped inverted in beta.6 and beta.7**. The source layers `kbd.css` so `InputGroupAddon`'s Tailwind utility keeps outranking `background: var(--input)`; unlayered, the plain declaration would have won instead. Rather than transcribe the losing declaration, `kbd.css` carries what the utility resolved to as a plain rule — `:where([data-slot="input-group"], .ds-input-group) .ds-kbd { background: color-mix(in oklab, var(--ds-ink-faint) 10%, transparent) }` — and drops the source's `var(--input)` line. Batch 7 has nothing to undo. |
| 6 | plan measurement wrong | `toolbar.css` carried `.ds-toolbar .ds-input { min-width: 200px }`, one of the **five** `.ds-input` descendant overrides batch 7 owes a propagation pass (batch-plan §4 batch 7, listing `color-picker.css:54`, `time-picker.css:13`, `toolbar.css:38`, `date-picker.css:16`, `date-range.css:17`). The wholesale replacement of `toolbar.css` deletes it — the replacement is `.ds-toolbar-input`, which sets its own width. **Batch 7 must reach four sites, not five.** |
| 6 | source omission, fixed forward | The source has **no indeterminate rendering for `progress`** — no stripe, no sliding segment, nothing keyed off Base UI's `data-indeterminate`. Absorbing it verbatim would have dropped a state 0.11 could draw and left `value={null}` as a blank track. `progress.css` keeps the animation, keyed off both `[data-indeterminate]` and 0.11's `--indeterminate` modifier, so the React binding and hand-written HTML land on the same rendering. |
| 6 | source defect, fixed forward | `empty.tsx`'s `EmptyDescription` was typed `React.ComponentProps<"p">` and rendered a `<div>`, and `EmptyMedia` rendered `data-slot="empty-icon"` under the name `media`. Both corrected in the absorbed component: a real `<p>`, and `data-slot="empty-media"`. |
| 6 | scope call, recorded | Three 0.11 renderings are **preserved** rather than replaced, each because a wholesale swap would have unstyled a committed fixture with no React component on either side to re-class it. (a) `table.css` keeps the whole element grammar — `.ds-table th/td`, `--hover`, `__num`, `__name`, `__row-action` — beside the source's part grammar, kept apart by `:not(.ds-table-head)` / `:not(.ds-table-cell)` guards and a `.ds-table-container > .ds-table` rule that drops the frame for the composed component; the plan names this one. (b) `spinner.css` keeps the drawn ring under `:not(svg)`, because the React binding spins a Phosphor glyph and hand-written HTML has no glyph to spin — without it every non-React consumer got an invisible rotating box. (c) `.ds-toolbar-spacer` survives the rename, because a hand-written full-width bar still has free space to eat even though the composed `<Toolbar>` is `width: fit-content`. |
| 6 | scope call, recorded | `stepper.tsx` renders **0.11's classes**, not the source's, because `stepper.css` is a cross-boundary pin held by `Wizard` — the plan requires this. Two consequences: there is **no `StepperSeparator`** (the connector is a `::after` on the step, so a separator element would style nothing), and `stepper.css` gains two additive blocks — `.ds-stepper__content { display: contents }` and a `[data-orientation="vertical"]` grid. Neither can reach `Wizard`, which renders no wrapper and no orientation attribute. |
| 6 | scope call, recorded | `examples/demo/` — the signed-in console — keeps the 0.11 prop shapes through a new demo-local `examples/demo/compat.js`, rather than rewriting ~35 hand-written `React.createElement` call sites onto the parts. Nothing in it ships (`examples/` is not in `package.json`'s `files`), and the component pages under `examples/components/` teach the real API. Rewriting the demo onto the parts is a 1.0 cutover item. |
| 6 | pre-existing, not a batch 6 regression | `tests/a11y.spec.js`'s `a11y: demo (signed-in console)` fails on `batch/5-charts-net-new` as well: the demo loads React from esm.sh at runtime and never reaches its sign-in button without network. Verified by stashing the batch and re-running. |
| 6 | incumbent defect, fixed forward | The incumbent `Checkbox` (held until batch 7) destructured a fixed prop list and dropped everything else, so `aria-label` never reached the `<input>`. A checkbox in a selection column has no visible text, so that is its only possible name — `table/selectable` failed axe's `label` rule on it. The applier now spreads its remaining props onto the input. Two lines, in a held component, because the alternative was a demo that fails the gate. |
| 6 | source rule dropped, deliberately | The source's `Stepper` makes the horizontal rail a tab stop, citing axe's `scrollable-region-focusable`: its own CSS lets the row overflow, and a rail with nothing focusable inside is then unreachable by keyboard. The held stylesheet gives every step `flex: 1 1 0; min-width: 0`, so the row shrinks rather than scrolling and the stop would have led nowhere. Dropped, and `site/tests/stepper-regress.spec.ts` asserts its absence alongside the connector. |
| 6 | global defect, fixed forward | `css/themes/dark.css` overrode every `--ds-*-bg` tint but none of the `--ds-*-ink` shades, which are tuned as dark text on the *light* tints. In the dark theme that left dark ink on a dark tint — `.ds-tag--success` and `.ds-banner--info` measured under 2:1. The four ink tokens now have dark-theme mirrors, each clearing 4.5:1 on its own tint, on `--ds-bg` and on `--ds-surface`. Fixed in the token layer rather than in `banner.css`, because `tag.css` and `chip.css` read the same family and `tag.css` is frozen — one four-line override fixes all three without touching a frozen contract. |
| 6 | pre-existing, newly exposed | The dark-theme `.ds-button--primary` contrast defect ([#15](https://github.com/LittleBigCode/design-system/issues/15)) now shows on `stepper` and `pagination` as well, because both batch 6 demos use a primary button. `button.css` is a frozen file batch 7 owns and is untouched here; the same failure already stands on `wizard`, `split-button`, `alert-dialog`, `toggle-group` and `theme-switcher`, none of which this batch touches. |
| 6 | pre-existing, harness | The docs site's own syntax highlighting (`.code-tokens`) fails axe's colour-contrast rule on **every** dark route, batch 1–5 routes included. It is the docs harness's code-sample rendering, not a shipped component, and it is not in #15's list — worth its own row there rather than being absorbed into a batch. |
| 7 | plan premise wrong, no fix needed | **`ds-open` and `ds-close` are not classes.** The plan and the CSS ledger call them "dead classes in a frozen surface" matched by no rule in `css/`, and ask batch 7 to implement or delete them (batch-plan §4 batch 7, §8 item 2; css-ledger §1). They are **CustomEvent names**: `ds-modal.js:67` and `:70` do `dispatchEvent(new CustomEvent("ds-open"))` and never `classList.add`, and both are documented as events in `components/README.md:37` and `docs/components.md:542`. The measurement was a grep artifact — a `ds-[a-z-]+` scan cannot tell a class literal from an event name. **Neither implemented nor deleted**: they are live public API and there is nothing to style. |
| 7 | plan measurement wrong | The `badge.css` **red strip** (batch-plan §1.4) is a no-op in the target, for the reason batch 0.3 recorded for the eleven held files: the only red in the file is `--accent`'s, it already routes through `--ds-accent` / `--ds-accent-ink`, and the modifier is named for it. The source's own `badge.css` brings no red either — its warning colour is `--destructive`. The 20-red-files count was measured on the source. |
| 7 | plan measurement wrong | The plan lists **16 forward cross-batch import re-wirings** owed their second half here (§4). Two of them have no React to re-wire: `message-scroller` (batch 2) and `carousel` (batch 3) are both **admit-CSS-only** — their stylesheets shipped and their bindings did not, because `@shadcn/react` and `embla-carousel-react` were never acquired. The plan's own §4 batch 2 and batch 3 notes say so; the forward-import table was counted from the source's imports, where the binding does exist. **14 re-wirings were owed and 14 are paid.** |
| 7 | react verdict overridden, recorded | **`icon` holds its React binding**, against the ledger's *source wins*. Same test §1.1 applied to `stat-card`: the premise fails. The source's `Icon` is a three-glyph registry (`bell`, `check`, `copy`) over `@phosphor-icons/react` against the incumbent's 34, and — decisively — it cannot serve `<ds-icon>` at all, which is zero-dependency JavaScript with no React to render and reads the same geometry map. Absorbing it would have swapped one shared map for a React-only registry plus a second copy of the geometry for the web component. What **is** absorbed is the substance of the task: the glyph set moves Lucide → Phosphor across the eight files §1.4 names, every icon *name* unchanged. |
| 7 | source rule dropped, deliberately | The source's `.ds-card` is a **flex column**, and absorbing that would have broken a published surface: `docs/streamlit.md` teaches `<div class="ds-card" style="padding:18px 20px">On track <span class="ds-tag ds-tag--success">Run</span></div>` — inline content with no parts, which a flex column stacks. `.ds-card` is frozen for exactly the case where the consumer supplies the children. The parts are block-level and stack unaided; the only thing the flex bought was the footer's push, which `.ds-card__footer` keeps as `margin-block-start: auto`. The same constraint kept `.ds-panel`'s padding on the root rather than moving it to the parts, because `<ds-panel>` also wraps arbitrary light DOM. |
| 7 | scope call, recorded | **Three class names carry two grammars each**, kept apart the way `table.css` kept its two in beta.6 — by what the element contains, not by load order. `.ds-checkbox` is 0.11's label-around-a-hidden-input when it has a `> .ds-checkbox__box` child and the absorbed Base UI box when it does not; `.ds-switch` splits on `> .ds-switch__track`; `.ds-input-group` on `> .ds-input-group__addon`. Each 0.11 form is rendered by committed fixtures under `examples/` that nothing in this migration re-classes, and `<ds-switch>` writes its form itself. `.ds-select` and `.ds-range` needed no guard: the source defines neither name, so the native wrapper and the native range sit beside `.ds-select-*` and `.ds-slider-*` without collision. |
| 7 | export renamed, recorded | **`Status` changes hands and the panel becomes `StatusPanel`.** 0.11's `Status` is a panel — a solid coloured head with a kicker, heading and subtitle — and the source's is an inline dot-and-label indicator. They are different components that happen to share a class stem, and the ledger gives the *name* to the source. In CSS both ship under `.ds-status`, told apart by whether the element has a `__head`. In React one symbol cannot be two things, so the panel takes the name its props interface (`StatusPanelProps`) and its stylesheet (`status-panel.css`) already used. `<ds-status>` is untouched: it is still the panel, and no web-component consumer moves. |
| 7 | incumbent defect, fixed forward | `react/index.tsx` exported **`type Status`**, the six-tone union. It could not be kept as a deprecated alias beside the new `Status` component: a type alias of that name in the barrel shadows the value for every consumer, and the site's own demos failed to compile on it. Renamed to `StatusTone` and recorded in `renames.json` rather than aliased. |
| 7 | plan item resolved elsewhere | The plan gives batch 7 the `.ds-input` **rename propagation** across five files. Two things reduce it to zero edits. `toolbar.css`'s override left with the wholesale replacement in beta.6 (recorded above), so four sites remain — `color-picker.css:54`, `time-picker.css:13`, `date-picker.css:16`, `date-range.css:17`. And the class is **not renamed**: source and target both call it `.ds-input`, so what moved is the file it is defined in, `field.css` → `input.css`. All four overrides win on specificity rather than load order and `input.css` loads ahead of every one of them, so none of the four was touched. `button-group.css:48` is a fifth descendant reference, added by beta.3 after the measurement, and is unaffected for the same reason. |
| 7 | pre-existing, not a batch 7 regression | `docs/for-claude.md` taught a switch as `<span class="ds-switch"><input type="checkbox" checked><span></span></span>` — the track span carrying no `ds-switch__track` class, so it was already unstyled before this batch. Corrected on the way past, because the `:has()` guard that tells the two switch grammars apart would have made a silent no-op into a visible one. |
| 7 | pre-existing, gap not closed here | `stat-card` is **incumbent holds** (ledger §1.1) and so should have been among the 30 held components batch 0.2 seeded into `registry.ts`. It is in `PENDING` instead, where nothing reads it — so the component ships in 1.0.0 with no route and no gate coverage. Left where it is rather than fixed opportunistically: it belongs to nobody's batch, and §5's cutover step 2 (assert the registry length equals 89 + 30) is the check that will catch it. Recorded here so it is not lost. |
| 7 | incumbent defect, fixed forward | **Issue [#15](https://github.com/LittleBigCode/design-system/issues/15)'s dark-theme contrast half is fixed**, on the batch that owns the files. `--ds-on-accent` is pinned to `--ds-blanc` and the dark theme does not flip it, so on dark's near-white `--ds-ink` every ink-filled surface rendered white on white. `bubble.css` recorded the finding in beta.2 and named `.ds-button--primary` as the outstanding case; `--ds-bg` is the token that inverts with the theme, which is what "the colour that contrasts with the ink" actually means. Applied to `.ds-button`'s `--btn-fg`, `.ds-badge--solid`, both of `checkbox.css`'s check marks, and `button-extras.css`'s loading spinner on the primary fill. The eight button *tones* keep `--ds-blanc` / `--ds-noir`: a tone is a brand colour, not a theme one. `split-button [dark]` is green as a side effect. |
| 7 | source defect, fixed forward | `field.css`'s error text used `var(--ds-danger)`, the base hue, which is tuned as a *fill*: as small text on this system's near-black dark background axe measured it under 4.5:1 on `field [dark]`. Moved to `--ds-danger-ink` — the mirror shade batch 6 added to `themes/dark.css` for exactly this — along with `.ds-field__hint--error` and `--success`, which had the same pairing. |
| 7 | pre-existing, still open | The docs harness's syntax highlighting (`.code-tokens`) fails axe's colour-contrast rule on **every** dark route, which now includes five of batch 7's. Batch 6 recorded it and it is unchanged: it is the docs app's own code-sample chrome, not a shipped component, and it does not appear in [#15](https://github.com/LittleBigCode/design-system/issues/15)'s list. It is the only violation left on this batch's 32 route/theme combinations, and §5's cutover step 1 — a full unfiltered gate — cannot pass until it has its own row there. |
| 8 | plan premise wrong, swap still earned | **The tooltip swap does not add `aria-describedby`, and cannot.** The plan states the defect as "`role="tooltip"` on a sibling span with no `id` and no `aria-describedby` on the trigger" and implies the absorbed component wires the description. Measured on the shipped build, Base UI 1.7.0's `Tooltip` emits **no `aria-describedby` and no `role="tooltip"` at all** — deliberately: its documented model is that a tooltip is a *visual-only* supplement and the trigger must carry its own accessible name closely matching the popup's text. The swap is still earned, by a different route: 0.11 announced an association that did not exist, which is worse than announcing none, and had neither Escape dismissal nor a hoverable popup (WCAG 1.4.13, both halves). `site/tests/tooltip-regress.spec.ts` pins what actually ships — every trigger independently named, no dangling `role="tooltip"`, Escape, hoverable — rather than the mechanism the plan guessed at. `tooltip/basic.tsx`'s trigger was relabelled on the way past: it said `aria-label="Delete"` under a tooltip reading "Move to archive", which gives the two audiences two different answers. |
| 8 | scope call, recorded | **The 0.11 drawer and tooltip grammars are preserved beside the absorbed ones**, the third and fourth instance of the call batch 6 recorded for `table.css`, `spinner.css` and `.ds-toolbar-spacer`. The ledger rules both files *wholesale replacement* on the test "not read by an external surface", which is about npm consumers; `examples/components/drawer.html`, `examples/components/tooltip.html`, `examples/kitchen-sink.html` and `examples/demo/views/files.js` write both grammars by hand, and **no React component on either side can re-class them**. `kitchen-sink.html` is in both root gates, so a wholesale swap would have unstyled a fixture the visual suite is about to baseline. Tooltip needed no guard — 0.11 is `.ds-tooltip` / `.ds-tooltip-host`, the source is `.ds-tooltip-content` / `-positioner` / `-arrow`, and the two share no name. Drawer shares exactly one, `.ds-drawer-overlay`, told apart by `:has(> .ds-drawer)` — beta.7's mechanism, on the same reasoning. `kanban` is the opposite case and IS replaced wholesale, because the plan gives this batch its three readers to re-class. |
| 8 | plan measurement incomplete | The plan lists **three** `examples/` files to re-class, all of them kanban's. Six more read a class this batch's ledger rows replace — `examples/components/drawer.html`, `examples/components/tooltip.html`, `examples/components/skeleton.html`, `examples/kitchen-sink.html`, `examples/demo/views/files.js`, plus `docs/components.md`, which is generated. None are re-classed, because the row above preserves their grammars instead; the three kanban files are re-classed exactly as the plan asks. Recorded because the count "three" was measured from the rename list, not from the readers. |
| 8 | source component unusable as absorbed, fixed forward | The source's `Skeleton` is a shadcn component sized entirely by Tailwind utilities (`h-4 w-full`), and **this package ships no utilities** — the same finding `breadcrumb.tsx`, `pagination.tsx` and `carousel.css` recorded for `sr-only`. Absorbed verbatim, `<Skeleton />` with no `className` renders a zero-height box, where 0.11's binding defaulted to `variant="line"`. `skeleton.css` — a HELD file — gains one additive rule giving an unmodified `.ds-skeleton` the line size, scoped `:not(:where([class*="ds-skeleton--"]))`. Both halves of that selector are load-bearing: without the `:not()` a bare rule placed after the modifiers silently outranks `--text`, which is the class `DataGrid` renders and the only reason the file is held; without the `:where()` the selector scores 0,2,0 and outranks any single-class rule a consumer writes to size one. Same shape as beta.6's two additive blocks on held `stepper.css`: it cannot reach the pinning reader, which always writes a modifier alongside. |
| 8 | gate precondition verified | **`DataGrid`'s loading state still renders**, the cross-boundary pin's check. `DataGrid.tsx:321` writes `"ds-skeleton ds-skeleton--text"` as a **string literal** and never composes the `Skeleton` component, so swapping the React binding cannot reach it by any path; `skeleton.css` is held and both classes are untouched. The pin is on the stylesheet alone. |
| 8 | dedupe exception resolved, not carried | `sheet.css`'s one exception was the source keeping `bg-popover`, `text-popover-foreground` and two `data-[side]:w-3/4` literals on `sheet.tsx`, because its `sidebar.tsx` overrode all three on a mobile `SheetContent` and `tailwind-merge` can only dedupe literals it can see. `sidebar` is incumbent-holds — the file never lands, only two cherry-picks onto `AppShell` do — so the override that made the exception necessary does not exist here. All four declarations came home to the stylesheet and `sheet.tsx` carries no Tailwind literals, which also finishes #157's C7: the two-file literal count is now zero. |
| 8 | source pattern adapted, recorded | **`sidebar`'s cookie cherry-pick is write-only in the source.** `sidebar.tsx` sets `sidebar_state` and never reads it: it is a Next.js pattern where a server component reads the cookie and feeds it back as `defaultOpen`. This package is buildless and has no server, so porting the write alone would have persisted nothing. `AppShell` reads it too, in a lazy initialiser, and only to seed the **uncontrolled** default — a caller passing `collapsed` still owns the state outright. The `⌘B` cherry-pick ports as-is, and the toggle gained `aria-keyshortcuts` so the shortcut is discoverable rather than merely present. |
| 8 | docs-harness defect, root-caused and fixed forward | **The `.code-tokens` colour-contrast failure is fixed**, at its cause. Batches 6 and 7 both recorded it as "the docs harness's own syntax highlighting" and deferred it, and beta.7's row named it the one thing blocking §5's cutover step 1. Measured rather than described, the failing spans were `#ff2a00` on **`#676768`** — and `shiki-theme.ts` computes every dark role against "`bg-muted`, i.e. `--ds-bg-alt` (#1b1c1f)". The surface was not the one the palette was tuned for. Cause: `chrome.css` bound `--color-muted: var(--ds-grey-bg)`, and **the dark theme overrides `--ds-bg-alt` but not `--ds-grey-bg`** (#ececec), so every `bg-muted` surface in the docs app was frozen at a near-white grey in dark — precisely what that block's own `@theme inline` note exists to prevent. One binding, `--color-muted: var(--ds-bg-alt)`, fixes every reader at once. The palette needed no edit: its ratios were right about the surface it named. |
| 8 | verified, no regression | The full unfiltered a11y suite (238 tests, every route x both themes) was run on this branch and on a clean worktree at the beta.7 commit, and the failure sets diffed by test name. **beta.7: 151 passed / 77 failed. beta.8: 216 passed / 22 failed. Zero newly-failing tests.** The 55 newly passing are all dark routes freed by the `bg-muted` fix above. The 22 that remain fail identically on both commits and belong to other batches: `aria-allowed-attr` on `time-picker` and `date-time-picker`, `label` on `tags-input` and `direction`, `aria-toggle-field-name` on `collapsible`, `label` and `marker`, and the dark-theme contrast of [#15](https://github.com/LittleBigCode/design-system/issues/15) on `.ds-button--danger`, `.ds-button--link`, `.ds-segmented` and four others. Recorded rather than fixed: none is this batch's surface, and §5's cutover step 1 is the gate that owns them. |
| 8 | pre-existing, not a batch 8 regression | Four assertions fail in `site/tests/batch1-rewiring-regress.spec.ts` (autocomplete's clear button) and `site/tests/batch3-rewiring-regress.spec.ts` (three on phone-input's native select). Verified pre-existing by running both specs in a clean worktree at `batch/7-frozen-contracts` HEAD: **the same four, by name, 47 passed / 4 failed on the baseline.** A worktree rather than a stash round-trip — beta.7 recorded that case-only renames corrupt through a stash on a case-insensitive filesystem, and this batch carries four of them. Not fixed here: they belong to batches 1 and 3, and the cutover's full unfiltered gate is where they are owed. |
| 8 | pre-existing, gap not closed here | **Fifteen batch-6 components have a `PLAYGROUNDS` entry and no playground file**, still sitting in `site/src/registry/_pending/playgrounds/` — `agenda`, `avatar`, `banner`, `breadcrumb`, `description-list`, `empty`, `kbd`, `page-header`, `pagination`, `progress`, `spinner`, `stepper`, `table`, `timeline`, `toolbar`. `playground-registry.ts` skips a slug with no file, so each of those pages renders its examples with no workbench. Batch 6 moved the demos and not the playgrounds; batch 7 did not notice. Left where they are for the same reason beta.7 left `stat-card`: they belong to nobody's batch, and moving them is a one-line `git mv` each that the cutover can do knowingly. This batch moved its own five. |
| 8 | queue spent, two items outstanding | With batch 8 resolved the `ready-for-agent` queue is empty, so the two items recorded as "belongs to nobody's batch" now belong to §5's cutover and to nothing else: **`stat-card`** is incumbent-holds and still sits in `PENDING`, where nothing reads it, so it would ship in 1.0.0 with no route and no gate coverage (beta.7's row); and the **fifteen stranded playgrounds** above. Cutover step 2 — assert `registry.ts` length equals 89 absorbed + 30 held — catches the first by construction. The second has no assertion behind it and is recorded here so it is not lost. |
| 8 | earlier correction wrong, root cause found | **Batch 6's diagnosis of the `a11y: demo (signed-in console)` failure is incorrect.** Its row says the demo "loads React from esm.sh at runtime and never reaches its sign-in button without network". It fails **with** network too, which is why it fails on CI, where the network is fine. The real cause is that `examples/demo.html`'s import map declares `react`, `react-dom`, `react-dom/client` and `recharts` and nothing else, while `dist/react/` is compiled with the automatic JSX runtime: **83 of its modules import `react/jsx-runtime`**, an unmapped bare specifier, so the barrel's module graph fails to resolve and the app never mounts. Behind it sit three more unmapped families — `@base-ui/react/*`, `@phosphor-icons/react` and, from this batch, `@dnd-kit/*`. Verified pre-existing: the identical `TypeError: Failed to resolve module specifier "react/jsx-runtime"` reproduces in a clean worktree at the beta.7 commit. **Not fixed here.** Adding the four map entries clears every resolution error and gets the app to its loader, but it then hangs on an esm.sh subpath that answers with HTML rather than a module, and chasing that is neither this batch's surface nor a shipped one (`examples/` is not in `package.json`'s `files`). Recorded so the next attempt starts from the import map rather than from the network. |
